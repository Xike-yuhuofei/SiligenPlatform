# Assets

本目录存放 `docs/` 全局共用的非文档资产。

## 子目录

- `images/`：全局共用图片资源。来源包括从历史文档区迁移的图片。
  - 使用方式：在 Markdown 文件中通过 `![描述](../assets/images/文件名.png)` 引用

## 使用规则

1. 本目录仅存放被多个文档共用的资产文件
2. 单篇文档专用的图片应放在该文档所在目录下
3. 新增图片时，文件名应尽量使用英文 kebab-case
