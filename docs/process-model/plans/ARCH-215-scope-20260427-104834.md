# ARCH-215 Scope - Residue Batch Freeze and Carry-Forward Entry

- 状态：Prepared
- 日期：2026-04-27
- 分支：`docs/infra/ARCH-215-batch-freeze`
- 工作流上下文：`ticket=ARCH-215`，`timestamp=20260427-104834`
- 来源：
  - `D:/Projects/wt-arch215`
  - `stash@{0}` on `fix/runtime/P0-PATH-02-path-quality-gate`
  - `stash@{1}` on `fix/runtime/P0-PATH-02-path-quality-gate`

## 1. 任务归类结论

- 类型：`boundary-audit`
- 归类依据：
  1. 当前主问题不是“某个单点功能未实现”，而是 `wt-arch215` 与 `P0-PATH-02` 残留同时承载了多个 owner、多个 contract 与多个历史清理意图，已经不适合继续在原载体上实施。
  2. `D:/Projects/wt-arch215` 对 `origin/main` 为 `ahead 0 / behind 52`，但工作区存在 70+ 文件未提交改动；`fix/runtime/P0-PATH-02-path-quality-gate` 对 `origin/main` 为 `ahead 0 / behind 39`，且仍挂两条混合 stash。两者都应先冻结边界，再迁出到新的正式任务分支。
  3. 用户已冻结原则：不兼容保留、不静默回退、不生产双轨；因此本轮必须先得到“单任务、单 owner、单 authority”的 carry-forward 入口，而不是继续在旧 worktree 中叠加补丁。

## 2. 本轮目标

1. 冻结 `wt-arch215` 的最小契约与批次边界，明确哪些残留可以形成正式任务，哪些只能阻塞保留。
2. 冻结 `P0-PATH-02` 两条 stash 的分类口径，明确哪些内容仍属于 `path_quality` 单轨 follow-up，哪些已经越界到 docs archive / motion cleanup / tooling residue。
3. 给后续实现者提供从 `origin/main` 继续施工的正式入口：推荐分支名、配对 worktree 名、最小验证与停止条件。

## 3. 非目标

1. 不在本任务中直接迁出 `wt-arch215` 的代码实现。
2. 不在本任务中 `apply/pop` 任一 stash。
3. 不在本任务中删除 `wt-arch215`、`fix/runtime/P0-PATH-02-path-quality-gate`、`backup/pr219-pre-rebase` 或任一远端分支。
4. 不在本任务中对旧分支做 rebase、force delete、history rewrite。

## 4. 最小契约冻结单

### 4.1 `ARCH-215-A`：job-ingest upload owner convergence

- 本轮语义对象：
  - upload authority artifact
  - upload public seam / consumer include boundary
- 输入工件：
  - `UploadRequest`
  - `IUploadStoragePort`
  - `IUploadPreparationPort`
- 输出工件：
  - `UploadResponse`
- 关键中间工件：
  - `UploadFileUseCase`
  - `module.yaml`
  - direct consumer include / namespace bindings
- authority 工件：
  - `UploadResponse`
- owner 层：
  - `modules/job-ingest`
- consumer 层：
  - runtime-service composition root
  - runtime-gateway facade / dispatcher
  - planner-cli / shared contracts tests
- 禁止重解释的层：
  - workflow legacy wrapper
  - storage seam 兼容层
  - PB concrete owner
- 真值依据：
  - `docs/decisions/job-ingest-upload-semantic-freeze.md`

### 4.2 `ARCH-215-B`：dispense-packaging planning / preview authority convergence

- 本轮语义对象：
  - planning assembly authority
  - preview snapshot / planning export authority
- 输入工件：
  - planning ports
  - process path / trigger layout planner inputs
- 输出工件：
  - planning use case export payload
  - preview snapshot authority artifacts
- 关键中间工件：
  - `PlanningUseCase`
  - `PreviewSnapshotService`
  - `preview_snapshot_fixture`
- authority 工件：
  - `dispense-packaging` 产出的 planning / preview authoritative payload
- owner 层：
  - `modules/dispense-packaging`
- consumer 层：
  - runtime-gateway
  - runtime-service
  - shared contract fixtures / test-kit
- 禁止重解释的层：
  - workflow 兼容壳
  - second preview response shape

### 4.3 `ARCH-215-C`：runtime-execution system / soft-limit convergence

- 本轮语义对象：
  - soft limit monitor application seam
  - initialize-system wiring authority
- 输入工件：
  - system initialization requests
  - host monitor services
- 输出工件：
  - initialize-system execution result
  - runtime monitor integration contract
- 关键中间工件：
  - `InitializeSystemUseCase`
  - `ISoftLimitMonitor`
  - `SoftLimitMonitorService`
- authority 工件：
  - `runtime-execution` application seam
- owner 层：
  - `modules/runtime-execution`
- consumer 层：
  - runtime-service composition root
  - host bootstrap smoke tests
- 禁止重解释的层：
  - container 层的旧 monitor wiring
  - host/runtime 双轨 monitor 接口

### 4.4 `P0-HMI`：path_quality production-blocked surface follow-up

- 本轮语义对象：
  - `path_quality` production gate 的 HMI/operator surface
  - `production-blocked` operator stage
- 输入工件：
  - `path_quality` authority payload
  - HMI preview/preflight status projection
- 输出工件：
  - HMI warning / blocked banner
  - operator context stage / HIL contract surface
- 关键中间工件：
  - `preview_payload_authority`
  - `preview_preflight`
  - operator runner summaries
- authority 工件：
  - `path_quality`
- owner 层：
  - upstream authority 仍是 runtime / planning 主链
  - HMI 只负责单轨传播，不新增 gate
- consumer 层：
  - `apps/hmi-app`
  - `modules/hmi-application`
  - HIL / operator contract tests
- 禁止重解释的层：
  - warning-only compatibility path
  - preview-only second gate

## 5. 当前 inventory 与批次分类

### 5.1 `wt-arch215`

- 当前状态：
  - worktree：`D:/Projects/wt-arch215`
  - 分支：`refactor/infra/ARCH-215-global-optimization`
  - upstream 关系：`origin/main`，`ahead 0 / behind 52`
  - 未提交改动：`71 files changed, 3063 insertions, 1405 deletions`
- ready-to-carry-forward batches：
  1. `ARCH-215-A`
     - 主要范围：`modules/job-ingest/**`、相关 direct consumers、`tests/contracts/test_job_ingest_semantic_freeze_contract.py`
     - 额外信号：
       - golden 从 `UploadResponse*` 迁往 `SourceDrawing*`
       - `shared/contracts/application/fixtures/responses/dxf.*.json` 可能只允许保留与 upload authority 直接相关的 hunk
  2. `ARCH-215-B`
     - 主要范围：`modules/dispense-packaging/**`、`shared/testing/test-kit/**`、preview/planning contract fixtures
  3. `ARCH-215-C`
     - 主要范围：`modules/runtime-execution/**InitializeSystem*`、`*SoftLimitMonitor*`、`apps/runtime-service/container/**`、`HostBootstrapSmokeTest.cpp`
- blocked / split-required residue：
  1. `ARCH-215-D`
     - 范围：`docs/architecture-audits/**`、`docs/audits/**`、历史审计迁移与索引调整
     - 当前结论：不能搭车功能批次；只有在形成“纯文档拓扑迁移”单一任务时才允许继续
  2. shared consumer files
     - 范围：`apps/runtime-gateway/**`、`apps/runtime-service/container/**`、`shared/contracts/**`
     - 当前结论：只能按 hunk 归属到 A/B/C；若同文件无法拆出单一 owner，就必须保持阻塞

### 5.2 `stash@{0}` on `fix/runtime/P0-PATH-02-path-quality-gate`

- 分类结果：
  1. `P0-HMI`
     - 主要范围：`apps/hmi-app/**`、`modules/hmi-application/**`、`tests/e2e/hardware-in-loop/**`、`tests/integration/scenarios/first-layer/test_hmi_operator_production_contract.py`
     - 目标：把现有 `path_quality` 单轨 gate 继续传播为 `production-blocked` surface，不新增第二套 authority
  2. `P0-DOC-ARCHIVE`
     - 主要范围：`docs/README.md`、`docs/_archive/**`、`docs/runtime/release-process.md`、`.agents/skills/test-static/SKILL.md`
     - 当前结论：与 `P0-HMI` 无法构成单一任务，必须独立阻塞
- 处理结论：
  - `stash@{0}` 不允许直接 pop 回 `P0-PATH-02` 分支
  - 只有 `P0-HMI` 子集允许被迁出到新的 follow-up 任务

### 5.3 `stash@{1}` on `fix/runtime/P0-PATH-02-path-quality-gate`

- 分类结果：`blocked-mixed`
- 混合内容：
  - docs archive / reviews cleanup
  - `modules/motion-planning/**`
  - `modules/dxf-geometry/application/engineering_data/processing/dxf_to_pb.py`
  - HMI/operator contract surface
- 处理结论：
  - 当前不得 pop、不得 closeout、不得挂回 `P0-PATH-02` 主线
  - 必须后续单独拆成“历史文档归档”、“motion cleanup”、“HMI surface”三个方向之一，否则保持 park

## 6. 推荐 carry-forward 任务设置

1. `ARCH-215-A`
   - 分支：`refactor/ingest/ARCH-215-upload-owner-convergence`
   - base：`origin/main`
   - worktree：`wt-arch215-3`
2. `ARCH-215-B`
   - 分支：`refactor/path/ARCH-215-preview-authority-convergence`
   - base：`origin/main`
   - worktree：`wt-arch215-4`
3. `ARCH-215-C`
   - 分支：`refactor/runtime/ARCH-215-soft-limit-monitor-convergence`
   - base：`origin/main`
   - worktree：`wt-arch215-5`
4. `P0-HMI`
   - 分支：`fix/hmi/TASK-004-production-blocked-surface`
   - base：`origin/main`
   - worktree：`wt-task004`

## 7. 每批最小验证

1. `ARCH-215-A`
   - `tests/contracts/test_job_ingest_semantic_freeze_contract.py`
   - `modules/job-ingest` contract / unit / integration tests
   - 任何直接改动到的 application/protocol compatibility tests
2. `ARCH-215-B`
   - `modules/dispense-packaging` unit / contract tests
   - `shared/testing/test-kit/src/test_kit/preview_snapshot_fixture.py` 相关 compatibility tests
3. `ARCH-215-C`
   - `modules/runtime-execution/tests/unit/system/InitializeSystemUseCaseTest.cpp`
   - `modules/runtime-execution/runtime/host/tests/unit/runtime/motion/SoftLimitMonitorServiceTest.cpp`
   - `apps/runtime-service/tests/integration/HostBootstrapSmokeTest.cpp`
4. `P0-HMI`
   - `apps/hmi-app/tests/unit/test_main_window.py`
   - `tests/integration/scenarios/first-layer/test_hmi_operator_production_contract.py`
   - `tests/integration/scenarios/first-layer/test_real_dxf_production_validation_contract.py`

## 8. 停止条件

1. 任一批次出现两个 authority owner、两个 public contract 或“功能收敛 + 大规模 docs archive”混批时，必须停止并继续拆分。
2. 任一共享文件无法按 hunk 明确归属到单一 batch 时，不得靠人工解释强行并批。
3. 若后续验证表明 `P0-HMI` 实际修改了 `path_quality` authority，而不仅是 surface 传播，则必须停止并另立任务。
4. 若后续要删除 `fix/runtime/P0-PATH-02-path-quality-gate` 或任一远端分支，必须先清空两条 stash 并通过仓库 pre-push delete-ref 安全门禁。

## 9. 下一步

1. 为 `ARCH-215-A`、`ARCH-215-B`、`ARCH-215-C`、`P0-HMI` 分别执行 `change-scope-guard`，冻结允许编辑的目录白名单。
2. 先从 `ARCH-215-A` 开始迁出，因为它已有明确的正式决策文档和最清晰的 owner artifact。
3. `ARCH-215-D` 与 `stash@{1}` 保持阻塞，直到能证明它们可以被拆成单任务 carry-forward。
