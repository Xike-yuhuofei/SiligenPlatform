# Architecture Audits

本目录默认属于历史文档区。

它承接专项审计、盘点、阶段性分析和重构过程证据。这些材料通常用于解释“曾经如何分析和收敛”，而不是直接承担当前正式真值。

使用规则：

1. `Codex` 默认不应把本目录作为当前架构规范的第一入口
2. 如果某份审计结论已经成为正式规则，应在 `docs/architecture/` 或 `docs/decisions/` 中形成正式文档
3. 本目录保留追溯和审计价值，但默认降权
