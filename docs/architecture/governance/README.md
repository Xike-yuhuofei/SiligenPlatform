# Governance

本目录承接工作区级治理类文档。

使用规则：

1. 当前 live 状态优先看根级 `legacy-cutover-status.md`。
2. migration、legacy、cutover 与 bridge exit 规则进入 `migration/`。
3. refactor guard 规则进入 `refactor-guard/`。
4. 边界拆分、shared/device seam 与 third-party 归口进入 `boundaries/`。

当前子目录：

- `migration/`
- `refactor-guard/`
- `boundaries/`
