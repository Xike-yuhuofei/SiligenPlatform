# Validation

本目录承接架构侧仍然有效的验证体系、门禁和长期基线文档。

读取规则：

1. 先看根级 `build-and-test.md`，确认工作区统一入口。
2. 再看本目录中的矩阵、门禁和性能基线。
3. 单次执行记录或阶段性验证结论仍应进入 `history/` 或 `docs/validation/history/`，不应回流到本目录。

当前正式文档：

- `layered-test-system-v2.md`
- `test-coverage-matrix-v1.md`
- `machine-preflight-quality-gate-v1.md`
- `performance-baseline.md`
