module_id: M3
module_name: topology-feature
owner_artifact: TopologyModel
group: upstream
target_root: modules/topology-feature
allowed_dependencies:
  - dxf-geometry/contracts
  - shared
uniform_skeleton_ready: true
notes: adapters/infrastructure/adapters/planning/geometry 是唯一真实实现承载面；SILIGEN_TARGET_IMPLEMENTATION_ROOTS 仅覆盖 contracts/adapters/tests；contracts/include 当前暴露的是 ContourAugmentConfig/ContourAugmenterAdapter seam，而非纯 owner contract
