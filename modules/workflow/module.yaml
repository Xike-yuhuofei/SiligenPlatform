module_id: M0
module_name: workflow
owner_artifact: WorkflowRun
group: workflow
target_root: modules/workflow
allowed_dependencies:
  - shared
  - */contracts
uniform_skeleton_ready: true
notes: contracts/include、application/include、domain/include、adapters/include 为 owner-backed canonical public surface；workflow/contracts 已落地 M0->M4 触发契约；domain/application 的 CMake 当前仅允许保留 bridge-only 聚合；workflow 已移除对 M6/M7/M8 application facade 的直接依赖，motion execution compatibility source 固定从 modules/motion-planning/domain/motion/ 解析，dispensing compatibility target 固定转发到 siligen_dispense_packaging_domain_dispensing；planning artifact export 的 result/interface consumer port owner 固定在 workflow/application，request 载荷固定消费 dispense-packaging/contracts，runtime concrete 固定由 runtime-execution/runtime/host 承接，并由 apps/runtime-service 完成装配
