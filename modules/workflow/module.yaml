module_id: M0
module_name: workflow
owner_artifact: WorkflowRun
group: workflow
target_root: modules/workflow
allowed_dependencies:
  - shared
  - */contracts
uniform_skeleton_ready: true
notes: >
  workflow/contracts 是当前稳定的 M0 触发契约面；recipe family 已迁到
  modules/recipe-lifecycle，跨模块事件发布契约已迁到
  shared/contracts/runtime/include/runtime/contracts/system/IEventPublisherPort.h。
  当前 live residue 主要集中在 domain/domain/dispensing bridge concrete、
  domain/include/domain/{motion,safety} compat headers、siligen_workflow_application_headers
  对 foreign owner surface 的透传，以及 tests/canonical 对 foreign targets 的依赖。
  services/examples 当前是 shell-only 保留目录；tests/canonical 是唯一允许继续承载
  source-bearing workflow tests 的目录。
