# Machine compatibility target removed.
# Canonical machine owner is runtime-execution / coordinate-alignment.
message(STATUS "[Domain::Machine] workflow no longer defines a machine compatibility target")
