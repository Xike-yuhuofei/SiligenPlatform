# services

`modules/dispense-packaging/services/` 当前仍是 shell-only 占位目录，不参与 live build。

当前 live 的 packaging / validation 编排入口实际位于
`modules/dispense-packaging/application/services/dispensing/`。
在 residual 尚未清完之前，不能把本目录写成已经承载 live owner service 的事实源。
