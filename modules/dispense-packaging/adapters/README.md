# adapters

`modules/dispense-packaging/adapters/` 当前仍是 shell-only 占位目录，不参与 live build。

本模块涉及的 runtime port / execution concrete 已在别的 owner 模块中继续收口；
本轮不把这里补成新的 live adapter root，也不借占位目录制造“已经完成拆分”的假象。
