# dispensing

承接点胶领域模型、补偿、触发规划、阀控编排，不吸入 TCP 或设备实现。

当前对应公开 target：`siligen_dispense_packaging_application_public`

`siligen_process_runtime_core_dispensing` 仅保留为 deprecated compatibility target，不再作为 live public surface。
