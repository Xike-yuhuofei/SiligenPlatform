module_id: M7
module_name: motion-planning
owner_artifact: MotionPlan
group: planning
target_root: modules/motion-planning
allowed_dependencies:
  - process-path/contracts
  - shared
uniform_skeleton_ready: true
notes: contracts/include + application/include 是当前 M7 正式 public surface；冻结对外入口为 MotionPlanningFacade、CmpInterpolationFacade、InterpolationProgramFacade、TrajectoryInterpolationFacade；业务阶段名保留 MotionPlan，但代码级 canonical payload 固定为 MotionTrajectory；InterpolationAlgorithm 真值固定在 contracts/include/motion_planning/contracts/InterpolationTypes.h；siligen_motion 只编译规划相关实现；workflow motion execution source 已固定从 modules/workflow/domain/domain/motion/ 解析
