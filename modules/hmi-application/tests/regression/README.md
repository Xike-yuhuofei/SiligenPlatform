# hmi-application regression tests

本目录当前仅为 workspace layout 要求的 canonical skeleton。

- 当前不承载 source-bearing regression 测试。
- 当前不进入 `hmi-application` canonical runner。
- 后续若补 fault/recovery 回归哨兵，只允许登记可重复执行的 `hmi-application` 自有回归验证。
