# hmi-application integration tests

本目录当前仅为 workspace layout 要求的 canonical skeleton。

- 当前不承载 source-bearing integration 测试。
- 当前不进入 `hmi-application` canonical runner。
- 后续若补 host-to-owner 邻接集成验证，只允许登记 `hmi-application` 自有的 integration 级测试。
