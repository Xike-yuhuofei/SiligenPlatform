# services

当前阶段 `services/` 仅作为后续可复用 runtime service / result assembly 的预留目录。

`runtime-execution` 的 live 编译 owner 仍以 `application/` 与 `runtime/host/` 为准；在未完成专门迁移前，不得把新的 broad orchestration / job control owner 重新落到此目录。
