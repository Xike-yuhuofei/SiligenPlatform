# job-ingest regression tests

本目录当前仅为 workspace layout 要求的 canonical skeleton。

- 当前不承载 source-bearing 测试。
- 当前不注册 `job-ingest` regression target。
- 后续若补 owner 级回归哨兵，只允许登记可重复、可定向执行的 `job-ingest` 自有回归验证。
