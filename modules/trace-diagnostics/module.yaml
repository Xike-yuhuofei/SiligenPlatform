module_id: M10
module_name: trace-diagnostics
owner_artifact: LoggingBootstrap
group: runtime
target_root: modules/trace-diagnostics
allowed_dependencies:
  - */contracts
  - shared
uniform_skeleton_ready: true
notes: adapters/logging/spdlog 是唯一真实实现承载面；contracts 同时承接 generic diagnostics sink；application/contracts 仅暴露 logging bootstrap surface；runtime bootstrap 通过 canonical application/contracts surface 消费模块能力，adapter 不对外暴露；hardware-test diagnostics contracts 不在本模块落地
