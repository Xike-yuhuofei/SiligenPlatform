module_id: M5
module_name: coordinate-alignment
owner_artifact: CoordinateTransformSet
group: planning
target_root: modules/coordinate-alignment
allowed_dependencies:
  - process-planning/contracts
  - shared
uniform_skeleton_ready: true
notes: siligen_module_coordinate_alignment 当前只暴露 contracts surface；CoordinateTransformSet 是当前唯一 M5 owner seam；contracts/include 仍携带 runtime-execution 依赖的 IHardwareTestPort residual；domain/machine 仅保留退出说明
