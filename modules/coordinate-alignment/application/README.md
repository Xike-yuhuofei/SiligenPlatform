# application

当前没有 live 的 `coordinate-alignment` application public surface。

- 本目录暂为 docs-only shell。
- `CoordinateTransformSet` 仍直接从 `contracts/` 暴露。
- 禁止在此重新引入仅做转发的 fake façade / provider / gateway 头文件。
