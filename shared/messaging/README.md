# shared/messaging

`shared/messaging/` 是共享消息语义基元的 canonical 子根，用于承接跨模块稳定的消息封装、传递约束与通用消息类型。

## 当前状态

- 本目录当前保留职责说明与目录占位，用于约束共享消息抽象的正式落点。
- 已稳定的底层消息类型与基础能力统一收敛在 `shared/kernel/`。
- 后续如果需要从公共内核中继续拆分消息专属语义，应直接在该 canonical 子根落地，而不是另建平行根。

## 禁止承载

- 网关协议解析实现、会话分发实现和设备传输适配实现
- 业务命令编排、流程状态机驱动与场景专属消息模型
