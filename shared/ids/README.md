# shared/ids

`shared/ids/` 是标识类基元的 canonical 子根，用于承接跨模块稳定的 ID/Key/Handle 类型约束。

## 当前状态

- 本目录当前保留职责说明与目录占位，用于约束共享标识语义的正式落点。
- 已稳定的公共基础类型继续统一收敛在 `shared/kernel/`。
- 后续如果从公共内核中拆分独立 ID/Key/Handle 语义，应直接在该 canonical 子根落地。

## 禁止承载

- 运行时宿主、设备适配或流程编排专属的业务标识解析逻辑
- 追溯查询、报警记录等业务模型本体
