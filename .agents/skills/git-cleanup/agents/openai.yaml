interface:
  display_name: "Git Cleanup"
  short_description: "Prune non-whitelisted worktrees and branches safely."
  default_prompt: "Use $git-cleanup to read the local whitelist file, inspect and classify stash entries first, dry-run cleanup candidates, and then safely remove non-whitelisted local worktrees, local branches, and remote branches without omitting any pending code changes."
