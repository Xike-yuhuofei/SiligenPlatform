interface:
  display_name: "Git Create"
  short_description: "Create and validate a compliant branch and worktree pair for a new task."
  default_prompt: "Use $git-create to create or validate a repository-compliant branch and worktree pair for a new task."
