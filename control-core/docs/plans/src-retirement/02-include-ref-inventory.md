# src/* include 与路径引用盘点

## 盘点范围
- 扫描目录：`apps/`、`modules/`、`tests/`、`src/`
- 扫描文件类型：`.h`、`.hpp`、`.cpp`
- 说明：仅盘点，不修改源码。

## 统计（相对 include 与绝对 include 分开）
- include_absolute_src: 70
- include_relative_src: 0
- hardcoded_absolute_src_path: 1
- hardcoded_relative_src_path: 0
- needs_decision 条目: 1

## 明细清单
| file | line | include_or_path | dependency_kind | suggested_fix_direction |
| --- | ---: | --- | --- | --- |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 12 | `"modules/device-hal/src/adapters/diagnostics/logging/SpdlogLoggingAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 14 | `"modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 15 | `"modules/device-hal/src/adapters/dispensing/dispenser/ValveAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 16 | `"modules/device-hal/src/adapters/dispensing/dispenser/triggering/TriggerControllerAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 17 | `"modules/device-hal/src/adapters/motion/controller/interpolation/InterpolationAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 18 | `"modules/device-hal/src/adapters/motion/controller/control/MultiCardMotionAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 19 | `"modules/device-hal/src/adapters/motion/controller/homing/HomingPortAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 20 | `"modules/device-hal/src/adapters/motion/controller/runtime/MotionRuntimeConnectionAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 21 | `"modules/device-hal/src/adapters/motion/controller/runtime/MotionRuntimeFacade.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 28 | `"modules/device-hal/src/adapters/recipes/RecipeFileRepository.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 29 | `"modules/device-hal/src/adapters/recipes/TemplateFileRepository.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 30 | `"modules/device-hal/src/adapters/recipes/AuditFileRepository.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 31 | `"modules/device-hal/src/adapters/recipes/ParameterSchemaFileProvider.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 32 | `"modules/device-hal/src/adapters/recipes/RecipeBundleSerializer.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 33 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 36 | `"modules/device-hal/src/drivers/multicard/MockMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 40 | `"modules/device-hal/src/drivers/multicard/RealMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp` | 295 | `"src/infrastructure/resources/config/files/recipes/schemas"` | `hardcoded_absolute_src_path_needs_decision` | 改为通过 modules/* 或 apps/* 的 canonical 资源入口注入（needs_decision：资源归属待定） |
| `apps/control-runtime/container/ApplicationContainer.Tcp.cpp` | 3 | `"modules/control-gateway/src/facades/tcp/TcpDispensingFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `apps/control-runtime/container/ApplicationContainer.Tcp.cpp` | 4 | `"modules/control-gateway/src/facades/tcp/TcpMotionFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `apps/control-runtime/container/ApplicationContainer.Tcp.cpp` | 5 | `"modules/control-gateway/src/facades/tcp/TcpRecipeFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `apps/control-runtime/container/ApplicationContainer.Tcp.cpp` | 6 | `"modules/control-gateway/src/facades/tcp/TcpSystemFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `apps/control-tcp-server/main.cpp` | 12 | `"modules/control-gateway/src/tcp/TcpCommandDispatcher.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `apps/control-tcp-server/main.cpp` | 13 | `"modules/control-gateway/src/tcp/TcpServer.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/JsonProtocol.cpp` | 1 | `"modules/control-gateway/src/tcp/JsonProtocol.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 1 | `"modules/control-gateway/src/tcp/TcpCommandDispatcher.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 9 | `"modules/control-gateway/src/facades/tcp/TcpDispensingFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 10 | `"modules/control-gateway/src/facades/tcp/TcpMotionFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 11 | `"modules/control-gateway/src/facades/tcp/TcpRecipeFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 12 | `"modules/control-gateway/src/facades/tcp/TcpSystemFacade.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpCommandDispatcher.cpp` | 15 | `"modules/device-hal/src/adapters/recipes/RecipeJsonSerializer.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpServer.cpp` | 1 | `"modules/control-gateway/src/tcp/TcpServer.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpServer.cpp` | 2 | `"modules/control-gateway/src/tcp/TcpSession.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpSession.cpp` | 1 | `"modules/control-gateway/src/tcp/TcpSession.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpSession.cpp` | 2 | `"modules/control-gateway/src/tcp/TcpServer.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/control-gateway/src/tcp/TcpSession.cpp` | 3 | `"modules/control-gateway/src/tcp/TcpCommandDispatcher.h"` | `include_absolute_src` | 改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/serializers/DiagnosticsJsonSerialization.h` | 15 | `"modules/device-hal/src/adapters/diagnostics/health/serializers/EnumConverters.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Cmp.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Connection.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Diagnostics.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Helpers.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Homing.cpp` | 7 | `"modules/device-hal/src/adapters/motion/controller/homing/HomingSupport.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Homing.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.IO.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Interpolation.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Jog.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Safety.cpp` | 7 | `"modules/device-hal/src/adapters/motion/controller/homing/HomingSupport.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Safety.cpp` | 16 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.Trigger.cpp` | 15 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.h` | 6 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/dispensing/dispenser/ValveAdapter.h` | 9 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/connection/HardwareConnectionAdapter.cpp` | 10 | `"modules/device-hal/src/drivers/multicard/MultiCardAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/connection/HardwareConnectionAdapter.h` | 7 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/connection/HardwareConnectionAdapter.h` | 8 | `"modules/device-hal/src/drivers/multicard/MultiCardAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/control/MultiCardMotionAdapter.h` | 11 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/control/MultiCardMotionAdapter.h` | 12 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/homing/HomingPortAdapter.cpp` | 7 | `"modules/device-hal/src/adapters/motion/controller/homing/HomingSupport.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/homing/HomingPortAdapter.cpp` | 14 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/homing/HomingPortAdapter.h` | 6 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/homing/HomingSupport.h` | 6 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/homing/HomingSupport.h` | 7 | `"modules/device-hal/src/drivers/multicard/MultiCardCPP.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/interpolation/InterpolationAdapter.h` | 4 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/runtime/MotionRuntimeConnectionAdapter.h` | 4 | `"modules/device-hal/src/adapters/motion/controller/runtime/MotionRuntimeFacade.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/motion/controller/runtime/MotionRuntimeFacade.h` | 6 | `"modules/device-hal/src/adapters/motion/controller/control/MultiCardMotionAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `modules/device-hal/src/adapters/recipes/RecipeJsonSerializer.cpp` | 1 | `"modules/device-hal/src/adapters/recipes/RecipeJsonSerializer.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.cpp` | 5 | `"modules/device-hal/src/drivers/multicard/MockMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.cpp` | 9 | `"modules/device-hal/src/drivers/multicard/RealMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.h` | 6 | `"modules/device-hal/src/adapters/motion/controller/connection/HardwareConnectionAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.h` | 7 | `"modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.h` | 8 | `"modules/device-hal/src/adapters/motion/controller/control/MultiCardMotionAdapter.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |
| `src/infrastructure/factories/InfrastructureAdapterFactory.h` | 9 | `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"` | `include_absolute_src` | 改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现） |

## 跨模块风险点
1. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:295` 引用 `"src/infrastructure/resources/config/files/recipes/schemas"`：硬编码 src 路径；apps/control-runtime -> src 跨模块耦合；needs_decision。建议：改为通过 modules/* 或 apps/* 的 canonical 资源入口注入（needs_decision：资源归属待定）
2. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:36` 引用 `"modules/device-hal/src/drivers/multicard/MockMultiCardWrapper.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
3. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:40` 引用 `"modules/device-hal/src/drivers/multicard/RealMultiCardWrapper.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
4. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:33` 引用 `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
5. `apps/control-runtime/container/ApplicationContainer.Tcp.cpp:3` 引用 `"modules/control-gateway/src/facades/tcp/TcpDispensingFacade.h"`：直连 *src/* include；apps/control-runtime -> modules/control-gateway 跨模块耦合。建议：改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现）
6. `apps/control-runtime/container/ApplicationContainer.Tcp.cpp:4` 引用 `"modules/control-gateway/src/facades/tcp/TcpMotionFacade.h"`：直连 *src/* include；apps/control-runtime -> modules/control-gateway 跨模块耦合。建议：改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现）
7. `apps/control-runtime/container/ApplicationContainer.Tcp.cpp:5` 引用 `"modules/control-gateway/src/facades/tcp/TcpRecipeFacade.h"`：直连 *src/* include；apps/control-runtime -> modules/control-gateway 跨模块耦合。建议：改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现）
8. `apps/control-runtime/container/ApplicationContainer.Tcp.cpp:6` 引用 `"modules/control-gateway/src/facades/tcp/TcpSystemFacade.h"`：直连 *src/* include；apps/control-runtime -> modules/control-gateway 跨模块耦合。建议：改为引用 modules/control-gateway 的 canonical 对外头（禁止直连 modules/control-gateway/src/* 内部实现）
9. `src/infrastructure/factories/InfrastructureAdapterFactory.cpp:5` 引用 `"modules/device-hal/src/drivers/multicard/MockMultiCardWrapper.h"`：直连 *src/* include；src -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
10. `src/infrastructure/factories/InfrastructureAdapterFactory.cpp:9` 引用 `"modules/device-hal/src/drivers/multicard/RealMultiCardWrapper.h"`：直连 *src/* include；src -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
11. `src/infrastructure/factories/InfrastructureAdapterFactory.h:9` 引用 `"modules/device-hal/src/drivers/multicard/IMultiCardWrapper.h"`：直连 *src/* include；src -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
12. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:12` 引用 `"modules/device-hal/src/adapters/diagnostics/logging/SpdlogLoggingAdapter.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
13. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:14` 引用 `"modules/device-hal/src/adapters/diagnostics/health/testing/HardwareTestAdapter.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
14. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:15` 引用 `"modules/device-hal/src/adapters/dispensing/dispenser/ValveAdapter.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
15. `apps/control-runtime/bootstrap/InfrastructureBindingsBuilder.cpp:16` 引用 `"modules/device-hal/src/adapters/dispensing/dispenser/triggering/TriggerControllerAdapter.h"`：直连 *src/* include；apps/control-runtime -> modules/device-hal 跨模块耦合。建议：改为引用 modules/device-hal 的 canonical 对外头（禁止直连 modules/device-hal/src/* 内部实现）
