# 限位控制安全功能测试示例

## 📋 概述

本文档提供运动控制卡限位控制安全功能的完整测试示例，包括硬件限位检测、软件限位配置、限位保护机制、限位报警处理、限位恢复、多轴限位协调、安全距离验证等核心功能的详细测试流程。

## 🎯 测试目标

- ✅ 验证硬件限位开关的检测可靠性
- ✅ 测试软件限位的使能/禁用功能
- ✅ 验证限位触发时的自动停止机制
- ✅ 测试限位报警的及时性和准确性
- ✅ 验证限位恢复和手动移出功能
- ✅ 测试多轴限位的协调保护
- ✅ 验证限位失效时的安全保护
- ✅ 测试限位参数的优化配置

## 🔧 Requirements

### 硬件要求
- **运动控制卡**: 已通过基础测试的控制卡
- **伺服系统**: 至少1轴完整的伺服系统
- **限位开关**: 正负限位开关（推荐使用机械式或接近式）
- **控制面板**: 急停按钮、复位按钮、限位复位开关
- **指示设备**: 限位状态LED指示灯或HMI显示
- **测量工具**: 万用表、示波器（用于信号测试）

### 限位开关要求
- **类型**: 机械限位开关、接近开关或光电开关
- **触发力**: 机械式 < 5N，非接触式检测距离合适
- **响应时间**: < 10ms
- **电气特性**: NPN/PNP输出，电压24V DC，电流> 10mA
- **机械寿命**: > 100万次操作
- **防护等级**: IP54或更高

### 机械要求
- **限位安装**: 安装位置准确，触发距离合理
- **机械行程**: 确保有限位触发前的安全缓冲距离
- **刚性要求**: 限位触发时机械结构无显著变形
- **运动精度**: 限位位置重复性 < ±0.1mm

### 安全要求
- **急停功能**: 急停按钮必须独立于限位系统
- **安全距离**: 限位触发前有足够的减速距离
- **冗余保护**: 软限位和硬限位双重保护
- **故障安全**: 限位失效时系统进入安全状态

### 预备条件
- [ ] 控制卡连接正常且通信稳定
- [ ] 伺服系统参数配置完成
- [ ] 限位开关安装固定，接线正确
- [ ] 限位信号线路无干扰，接地良好
- [ ] 机械润滑良好，运动顺畅

---

## 🔄 完整控制流程

### 1. 系统初始化和安全检查

#### 1.1 系统初始化
```cpp
#include "MultiCardCPP.h"
#include <vector>

MultiCard g_MultiCard;  // 全局控制卡对象

// 限位测试结果结构
struct LimitTestResult {
    int axisNum;
    bool positiveLimitWorking;
    bool negativeLimitWorking;
    bool positiveLimitEnabled;
    bool negativeLimitEnabled;
    long positiveLimitPosition;
    long negativeLimitPosition;
    DWORD positiveTriggerTime;
    DWORD negativeTriggerTime;
    int positiveAlarmCount;
    int negativeAlarmCount;
    std::string testStatus;
};

std::vector<LimitTestResult> g_limitTestResults;

int InitializeLimitControlSystem()
{
    printf("=== 限位控制安全功能测试 ===\n\n");

    // 建立控制卡连接
    int result = g_MultiCard.MC_Open(1, "192.168.0.200", 60000, "192.168.0.1", 60000);
    if (result != 0)
    {
        printf("❌ 控制卡连接失败，错误码: %d\n", result);
        return result;
    }

    printf("✅ 控制卡连接成功\n");

    // 复位控制卡
    g_MultiCard.MC_Reset();
    printf("✅ 控制卡复位完成\n");

    // 初始化测试结果
    g_limitTestResults.clear();
    for (int i = 1; i <= 16; i++)
    {
        LimitTestResult result;
        result.axisNum = i;
        result.positiveLimitWorking = false;
        result.negativeLimitWorking = false;
        result.positiveLimitEnabled = false;
        result.negativeLimitEnabled = false;
        result.positiveLimitPosition = 0;
        result.negativeLimitPosition = 0;
        result.positiveTriggerTime = 0;
        result.negativeTriggerTime = 0;
        result.positiveAlarmCount = 0;
        result.negativeAlarmCount = 0;
        result.testStatus = "未测试";
        g_limitTestResults.push_back(result);
    }

    return 0;
}
```

#### 1.2 限位硬件安全检查
```cpp
int PerformLimitHardwareSafetyCheck()
{
    printf("\n执行限位硬件安全检查...\n");

    // 读取所有轴的初始状态
    TAllSysStatusDataSX statusData;
    int result = g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    if (result != 0)
    {
        printf("❌ 无法读取轴状态\n");
        return result;
    }

    int initialAlarmCount = 0;
    int initialLimitCount = 0;

    printf("初始限位状态检查:\n");
    for (int i = 0; i < 8; i++) // 检查前8轴
    {
        DWORD axisStatus = statusData.lAxisStatus[i];
        bool posLimit = (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;
        bool negLimit = (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

        if (posLimit)
        {
            initialLimitCount++;
            printf("⚠️ 轴%d触发正限位 - 请检查机械位置\n", i + 1);
            g_limitTestResults[i].positiveLimitWorking = true;
        }

        if (negLimit)
        {
            initialLimitCount++;
            printf("⚠️ 轴%d触发负限位 - 请检查机械位置\n", i + 1);
            g_limitTestResults[i].negativeLimitWorking = true;
        }

        if (axisStatus & AXIS_STATUS_SV_ALARM)
        {
            initialAlarmCount++;
            printf("⚠️ 轴%d存在伺服报警\n", i + 1);
        }

        // 显示原始限位信号状态
        bool rawPosLimit = (statusData.lLimitPosRaw & (0x0001 << i)) != 0;
        bool rawNegLimit = (statusData.lLimitNegRaw & (0x0001 << i)) != 0;

        if (i < 4) // 只显示前4轴的详细信息
        {
            printf("  轴%d: 正限位=%s, 负限位=%s (原始信号: 正=%s, 负=%s)\n",
                   i + 1,
                   posLimit ? "触发" : "正常",
                   negLimit ? "触发" : "正常",
                   rawPosLimit ? "ON" : "OFF",
                   rawNegLimit ? "ON" : "OFF");
        }
    }

    printf("\n硬件安全检查结果:\n");
    printf("  初始限位触发轴数: %d\n", initialLimitCount);
    printf("  初始伺服报警轴数: %d\n", initialAlarmCount);

    if (initialAlarmCount > 0)
    {
        printf("⚠️ 存在伺服报警，请检查驱动器和电机\n");
    }

    // 检查急停状态
    DWORD gpiStatus = statusData.lGpiRaw[0];
    if (gpiStatus & 0x8000) // 假设急停连接到最高位
    {
        printf("❌ 急停信号激活，无法进行限位测试\n");
        return -1;
    }
    else
    {
        printf("✅ 急停信号正常\n");
    }

    return 0;
}
```

### 2. 限位开关基础功能测试

#### 2.1 限位开关状态检测测试
```cpp
int TestLimitSwitchDetection(int axisNum)
{
    printf("\n=== 限位开关检测测试 ===\n");
    printf("测试轴: %d\n", axisNum);

    // 确保轴未使能
    g_MultiCard.MC_AxisOff(axisNum);
    Sleep(1000);

    // 检查限位使能状态
    short posLimitEnabled, negLimitEnabled;
    int result = g_MultiCard.MC_GetLmtsOnOff(axisNum, &posLimitEnabled, &negLimitEnabled);
    if (result != 0)
    {
        printf("❌ 无法读取限位使能状态，错误码: %d\n", result);
        return result;
    }

    printf("当前限位使能状态:\n");
    printf("  正限位: %s\n", posLimitEnabled ? "启用" : "禁用");
    printf("  负限位: %s\n", negLimitEnabled ? "启用" : "禁用");

    g_limitTestResults[axisNum-1].positiveLimitEnabled = posLimitEnabled;
    g_limitTestResults[axisNum-1].negativeLimitEnabled = negLimitEnabled;

    // 如果限位被禁用，先启用
    if (!posLimitEnabled)
    {
        printf("启用正限位...\n");
        g_MultiCard.MC_LmtsOn(axisNum, MC_LIMIT_POSITIVE);
        Sleep(500);
    }

    if (!negLimitEnabled)
    {
        printf("启用负限位...\n");
        g_MultiCard.MC_LmtsOn(axisNum, MC_LIMIT_NEGATIVE);
        Sleep(500);
    }

    // 手动测试限位开关
    printf("\n手动测试限位开关:\n");
    printf("请手动触发轴%d的正限位开关...\n", axisNum);
    printf("按Enter键继续（或输入'skip'跳过此轴）...\n");

    char input[20];
    fgets(input, sizeof(input), stdin);
    if (strncmp(input, "skip", 4) == 0)
    {
        printf("跳过轴%d的限位测试\n", axisNum);
        return 1;
    }

    // 检测正限位触发
    return TestPositiveLimitTrigger(axisNum);
}
```

#### 2.2 正限位触发测试
```cpp
int TestPositiveLimitTrigger(int axisNum)
{
    printf("检测正限位触发...\n");

    DWORD startTime = GetTickCount();
    bool limitTriggered = false;
    int triggerCount = 0;

    // 监控限位状态变化
    for (int i = 0; i < 1000; i++) // 最多监控10秒
    {
        TAllSysStatusDataSX statusData;
        int result = g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        if (result != 0)
        {
            printf("❌ 读取状态失败\n");
            return result;
        }

        DWORD axisStatus = statusData.lAxisStatus[axisNum-1];
        bool currentLimitState = (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;

        // 检测限位触发
        if (currentLimitState && !limitTriggered)
        {
            DWORD triggerTime = GetTickCount() - startTime;
            printf("✅ 正限位触发！触发时间: %ld ms\n", triggerTime);

            // 记录当前位置
            long currentPos = statusData.lAxisPrfPos[axisNum-1];
            printf("  触发位置: %ld\n", currentPos);

            g_limitTestResults[axisNum-1].positiveLimitWorking = true;
            g_limitTestResults[axisNum-1].positiveLimitPosition = currentPos;
            g_limitTestResults[axisNum-1].positiveTriggerTime = triggerTime;
            g_limitTestResults[axisNum-1].positiveAlarmCount++;

            limitTriggered = true;
            triggerCount++;
        }

        // 检测限位释放
        if (!currentLimitState && limitTriggered)
        {
            DWORD releaseTime = GetTickCount() - startTime;
            printf("  正限位释放，释放时间: %ld ms\n", releaseTime);
            break;
        }

        // 实时显示状态
        static DWORD lastDisplayTime = 0;
        DWORD currentTime = GetTickCount();
        if (currentTime - lastDisplayTime > 500) // 每500ms显示一次
        {
            printf("  等待限位触发... 已等待 %ld ms\n", currentTime - startTime);
            lastDisplayTime = currentTime;
        }

        Sleep(50);
    }

    if (!limitTriggered)
    {
        printf("❌ 正限位未触发（可能原因：限位开关故障、接线问题、或未手动触发）\n");
        printf("建议检查：\n");
        printf("  1. 限位开关是否正常工作\n");
        printf("  2. 接线是否正确连接\n");
        printf("  3. 限位开关是否被手动触发\n");
        return -1;
    }

    // 测试负限位（可选）
    printf("\n是否测试负限位？(y/n): ");
    char choice;
    scanf(" %c", &choice);
    getchar(); // 清除输入缓冲区

    if (choice == 'y' || choice == 'Y')
    {
        printf("请手动触发轴%d的负限位开关...\n", axisNum);
        getchar(); // 等待用户触发

        return TestNegativeLimitTrigger(axisNum);
    }

    return 0;
}
```

#### 2.3 负限位触发测试
```cpp
int TestNegativeLimitTrigger(int axisNum)
{
    printf("检测负限位触发...\n");

    DWORD startTime = GetTickCount();
    bool limitTriggered = false;

    for (int i = 0; i < 1000; i++) // 最多监控10秒
    {
        TAllSysStatusDataSX statusData;
        int result = g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        if (result != 0)
        {
            printf("❌ 读取状态失败\n");
            return result;
        }

        DWORD axisStatus = statusData.lAxisStatus[axisNum-1];
        bool currentLimitState = (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

        if (currentLimitState && !limitTriggered)
        {
            DWORD triggerTime = GetTickCount() - startTime;
            printf("✅ 负限位触发！触发时间: %ld ms\n", triggerTime);

            long currentPos = statusData.lAxisPrfPos[axisNum-1];
            printf("  触发位置: %ld\n", currentPos);

            g_limitTestResults[axisNum-1].negativeLimitWorking = true;
            g_limitTestResults[axisNum-1].negativeLimitPosition = currentPos;
            g_limitTestResults[axisNum-1].negativeTriggerTime = triggerTime;
            g_limitTestResults[axisNum-1].negativeAlarmCount++;

            limitTriggered = true;
            break;
        }

        Sleep(50);
    }

    if (!limitTriggered)
    {
        printf("❌ 负限位未触发\n");
        return -1;
    }

    printf("✅ 轴%d限位开关检测完成\n", axisNum);
    g_limitTestResults[axisNum-1].testStatus = "检测完成";
    return 0;
}
```

### 3. 限位保护机制测试

#### 3.1 限位自动停止测试
```cpp
int TestLimitAutoStop(int axisNum)
{
    printf("\n=== 限位自动停止测试 ===\n");
    printf("测试轴: %d\n", axisNum);

    // 检查限位开关是否正常工作
    if (!g_limitTestResults[axisNum-1].positiveLimitWorking &&
        !g_limitTestResults[axisNum-1].negativeLimitWorking)
    {
        printf("❌ 限位开关未通过检测，无法进行自动停止测试\n");
        return -1;
    }

    // 确保限位已启用
    g_MultiCard.MC_LmtsOn(axisNum, MC_LIMIT_POSITIVE);
    g_MultiCard.MC_LmtsOn(axisNum, MC_LIMIT_NEGATIVE);
    Sleep(500);

    // 使能轴
    printf("使能轴%d...\n", axisNum);
    g_MultiCard.MC_AxisOn(axisNum);
    Sleep(1000);

    // 移动到安全位置
    long safePosition = 0;
    if (g_limitTestResults[axisNum-1].positiveLimitPosition > 0)
    {
        safePosition = g_limitTestResults[axisNum-1].positiveLimitPosition - 20000;
    }
    else if (g_limitTestResults[axisNum-1].negativeLimitPosition < 0)
    {
        safePosition = g_limitTestResults[axisNum-1].negativeLimitPosition + 20000;
    }

    printf("移动到安全位置: %ld\n", safePosition);
    TestTrapMotion(axisNum, safePosition, 10, 0.2, 0.2);
    MonitorMotion(axisNum);
    Sleep(1000);

    // 测试向限位方向运动
    if (g_limitTestResults[axisNum-1].positiveLimitWorking)
    {
        printf("测试正限位自动停止...\n");
        return TestPositiveLimitAutoStop(axisNum);
    }
    else if (g_limitTestResults[axisNum-1].negativeLimitWorking)
    {
        printf("测试负限位自动停止...\n");
        return TestNegativeLimitAutoStop(axisNum);
    }
    else
    {
        printf("❌ 无可用的限位开关进行测试\n");
        return -1;
    }
}
```

#### 3.2 正限位自动停止测试
```cpp
int TestPositiveLimitAutoStop(int axisNum)
{
    printf("向正限位方向运动，测试自动停止功能...\n");

    // 读取起始状态
    TAllSysStatusDataSX statusData;
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    long startPos = statusData.lAxisPrfPos[axisNum-1];
    DWORD startTime = GetTickCount();

    // 启动向正方向的运动
    TestTrapMotion(axisNum, startPos + 100000, 15, 0.3, 0.3);

    // 监控限位触发和停止过程
    bool limitTriggered = false;
    bool motionStopped = false;
    long stopPosition = 0;
    DWORD stopTime = 0;

    for (int i = 0; i < 2000; i++) // 最多监控20秒
    {
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        DWORD axisStatus = statusData.lAxisStatus[axisNum-1];
        long currentPos = statusData.lAxisPrfPos[axisNum-1];

        // 检测限位触发
        if ((axisStatus & AXIS_STATUS_POS_HARD_LIMIT) && !limitTriggered)
        {
            DWORD triggerTime = GetTickCount() - startTime;
            printf("  正限位触发，触发时间: %ld ms，位置: %ld\n", triggerTime, currentPos);
            limitTriggered = true;
        }

        // 检测运动停止
        if (!(axisStatus & AXIS_STATUS_RUNNING) && limitTriggered && !motionStopped)
        {
            stopTime = GetTickCount() - startTime;
            stopPosition = currentPos;
            printf("  运动自动停止，停止时间: %ld ms，停止位置: %ld\n", stopTime, stopPosition);
            motionStopped = true;
        }

        // 实时显示状态
        static DWORD lastDisplayTime = 0;
        DWORD currentTime = GetTickCount();
        if (currentTime - lastDisplayTime > 200)
        {
            printf("  位置: %ld, 状态: 限位=%s, 运行=%s\n",
                   currentPos,
                   (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) ? "触发" : "正常",
                   (axisStatus & AXIS_STATUS_RUNNING) ? "运行" : "停止");
            lastDisplayTime = currentTime;
        }

        if (motionStopped)
        {
            break;
        }

        Sleep(50);
    }

    // 分析停止效果
    if (motionStopped)
    {
        DWORD stopDelay = stopTime - (startTime + 2000); // 减去初始的2秒运动时间
        printf("正限位自动停止测试结果:\n");
        printf("  限位触发到停止的延迟: %ld ms\n", stopDelay);
        printf("  停止位置与限位位置差: %ld\n",
               stopPosition - g_limitTestResults[axisNum-1].positiveLimitPosition);

        if (stopDelay < 1000) // 1秒内停止
        {
            printf("✅ 正限位自动停止功能正常\n");
            return 0;
        }
        else
        {
            printf("⚠️ 停止延迟较长，可能需要优化参数\n");
            return 1;
        }
    }
    else
    {
        printf("❌ 正限位自动停止功能异常\n");
        return -1;
    }
}
```

#### 3.3 负限位自动停止测试
```cpp
int TestNegativeLimitAutoStop(int axisNum)
{
    printf("向负限位方向运动，测试自动停止功能...\n");

    // 读取起始状态
    TAllSysStatusDataSX statusData;
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    long startPos = statusData.lAxisPrfPos[axisNum-1];
    DWORD startTime = GetTickCount();

    // 启动向负方向的运动
    TestTrapMotion(axisNum, startPos - 100000, 15, 0.3, 0.3);

    // 监控限位触发和停止过程
    bool limitTriggered = false;
    bool motionStopped = false;
    long stopPosition = 0;
    DWORD stopTime = 0;

    for (int i = 0; i < 2000; i++) // 最多监控20秒
    {
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        DWORD axisStatus = statusData.lAxisStatus[axisNum-1];
        long currentPos = statusData.lAxisPrfPos[axisNum-1];

        // 检测限位触发
        if ((axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) && !limitTriggered)
        {
            DWORD triggerTime = GetTickCount() - startTime;
            printf("  负限位触发，触发时间: %ld ms，位置: %ld\n", triggerTime, currentPos);
            limitTriggered = true;
        }

        // 检测运动停止
        if (!(axisStatus & AXIS_STATUS_RUNNING) && limitTriggered && !motionStopped)
        {
            stopTime = GetTickCount() - startTime;
            stopPosition = currentPos;
            printf("  运动自动停止，停止时间: %ld ms，停止位置: %ld\n", stopTime, stopPosition);
            motionStopped = true;
        }

        // 实时显示状态
        static DWORD lastDisplayTime = 0;
        DWORD currentTime = GetTickCount();
        if (currentTime - lastDisplayTime > 200)
        {
            printf("  位置: %ld, 状态: 限位=%s, 运行=%s\n",
                   currentPos,
                   (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) ? "触发" : "正常",
                   (axisStatus & AXIS_STATUS_RUNNING) ? "运行" : "停止");
            lastDisplayTime = currentTime;
        }

        if (motionStopped)
        {
            break;
        }

        Sleep(50);
    }

    // 分析停止效果
    if (motionStopped)
    {
        printf("✅ 负限位自动停止功能正常\n");
        return 0;
    }
    else
    {
        printf("❌ 负限位自动停止功能异常\n");
        return -1;
    }
}
```

### 4. 限位恢复测试

#### 4.1 手动移出限位测试
```cpp
int TestLimitRecovery(int axisNum)
{
    printf("\n=== 限位恢复测试 ===\n");
    printf("测试轴: %d\n", axisNum);

    // 检查当前限位状态
    TAllSysStatusDataSX statusData;
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    DWORD axisStatus = statusData.lAxisStatus[axisNum-1];

    bool posLimitActive = (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;
    bool negLimitActive = (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

    if (!posLimitActive && !negLimitActive)
    {
        printf("当前无限位触发，需要先触发限位进行恢复测试\n");
        printf("是否先触发限位？(y/n): ");
        char choice;
        scanf(" %c", &choice);
        getchar();

        if (choice == 'y' || choice == 'Y')
        {
            printf("请手动触发限位，然后按Enter键...\n");
            getchar();

            g_MultiCard.MC_GetAllSysStatusSX(&statusData);
            axisStatus = statusData.lAxisStatus[axisNum-1];
            posLimitActive = (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;
            negLimitActive = (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

            if (!posLimitActive && !negLimitActive)
            {
                printf("❌ 未检测到限位触发\n");
                return -1;
            }
        }
        else
        {
            printf("跳过限位恢复测试\n");
            return 1;
        }
    }

    // 尝试移出限位区域
    printf("尝试手动移出限位区域...\n");
    printf("请手动移出限位开关的触发范围\n");
    printf("按Enter键继续...\n");
    getchar();

    // 监控限位释放过程
    DWORD startTime = GetTickCount();
    bool limitReleased = false;

    for (int i = 0; i < 1000; i++) // 最多监控10秒
    {
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        DWORD newAxisStatus = statusData.lAxisStatus[axisNum-1];

        bool newPosLimitActive = (newAxisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;
        bool newNegLimitActive = (newAxisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

        if ((posLimitActive && !newPosLimitActive) ||
            (negLimitActive && !newNegLimitActive))
        {
            DWORD releaseTime = GetTickCount() - startTime;
            printf("✅ 限位已释放，释放时间: %ld ms\n", releaseTime);
            limitReleased = true;
            break;
        }

        Sleep(100);
    }

    if (limitReleased)
    {
        // 清除轴状态
        printf("清除轴状态...\n");
        g_MultiCard.MC_ClrSts(axisNum);
        Sleep(500);

        // 测试恢复后的运动能力
        printf("测试恢复后的运动能力...\n");
        long currentPos = statusData.lAxisPrfPos[axisNum-1];
        long testPos = currentPos + 5000; // 向正方向移动5mm

        TestTrapMotion(axisNum, testPos, 10, 0.2, 0.2);
        MonitorMotion(axisNum);

        printf("✅ 限位恢复测试通过\n");
        g_limitTestResults[axisNum-1].testStatus = "恢复测试完成";
        return 0;
    }
    else
    {
        printf("❌ 限位未释放，请检查机械位置\n");
        return -1;
    }
}
```

### 5. 软件限位功能测试

#### 5.1 软件限位配置测试
```cpp
int TestSoftwareLimitConfiguration(int axisNum)
{
    printf("\n=== 软件限位配置测试 ===\n");
    printf("测试轴: %d\n", axisNum);

    // 读取当前轴位置作为中心
    TAllSysStatusDataSX statusData;
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    long centerPos = statusData.lAxisPrfPos[axisNum-1];

    printf("当前轴位置: %ld\n", centerPos);

    // 定义软件限位范围（±20000 pulse，约±20mm）
    long softLimitPositive = centerPos + 20000;
    long softLimitNegative = centerPos - 20000;

    printf("设置软件限位范围:\n");
    printf("  正向软限位: %ld\n", softLimitPositive);
    printf("  负向软限位: %ld\n", softLimitNegative);

    // 配置软件限位
    printf("配置软件限位...\n");

    // 注意：MultiCard可能需要使用特定的API来设置软限位
    // 这里提供的是概念性代码，实际API需要根据具体硬件文档调整

    // 模拟软件限位设置（实际使用时需要替换为正确的API）
    printf("  设置正向软限位...\n");
    // int result = g_MultiCard.MC_SetSoftLimit(axisNum, MC_LIMIT_POSITIVE, softLimitPositive);
    // if (result != 0) {
    //     printf("❌ 正向软限位设置失败，错误码: %d\n", result);
    //     return result;
    // }

    printf("  设置负向软限位...\n");
    // result = g_MultiCard.MC_SetSoftLimit(axisNum, MC_LIMIT_NEGATIVE, softLimitNegative);
    // if (result != 0) {
    //     printf("❌ 负向软限位设置失败，错误码: %d\n", result);
    //     return result;
    // }

    printf("✅ 软件限位配置完成（概念性代码，需要根据实际API调整）\n");

    // 测试软件限位效果
    return TestSoftwareLimitEffect(axisNum, softLimitPositive, softLimitNegative);
}
```

#### 5.2 软件限位效果测试
```cpp
int TestSoftwareLimitEffect(int axisNum, long posLimit, long negLimit)
{
    printf("测试软件限位效果...\n");

    // 确保轴已使能
    g_MultiCard.MC_AxisOn(axisNum);
    Sleep(1000);

    // 测试正向软件限位
    printf("测试正向软件限位...\n");

    // 尝试移动超过正向软件限位
    long testPos = posLimit + 5000; // 超过软限位5mm
    printf("尝试移动到位置: %ld (超过软限位)\n", testPos);

    TestTrapMotion(axisNum, testPos, 20, 0.5, 0.5);

    // 监控运动停止
    Sleep(3000);
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    long actualPos = statusData.lAxisPrfPos[axisNum-1];

    printf("实际到达位置: %ld\n", actualPos);

    if (actualPos <= posLimit)
    {
        printf("✅ 正向软件限位生效，运动在 %ld 处停止\n", actualPos);
    }
    else
    {
        printf("⚠️ 正向软件限位可能未生效\n");
    }

    // 测试负向软件限位
    printf("测试负向软件限位...\n");

    testPos = negLimit - 5000; // 超过负软限位5mm
    printf("尝试移动到位置: %ld (超过软限位)\n", testPos);

    TestTrapMotion(axisNum, testPos, 20, 0.5, 0.5);

    // 监控运动停止
    Sleep(3000);
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    actualPos = statusData.lAxisPrfPos[axisNum-1];

    printf("实际到达位置: %ld\n", actualPos);

    if (actualPos >= negLimit)
    {
        printf("✅ 负向软件限位生效，运动在 %ld 处停止\n", actualPos);
    }
    else
    {
        printf("⚠️ 负向软件限位可能未生效\n");
    }

    // 移回安全位置
    printf("移回安全位置...\n");
    long safePos = (posLimit + negLimit) / 2;
    TestTrapMotion(axisNum, safePos, 10, 0.3, 0.3);
    MonitorMotion(axisNum);

    printf("✅ 软件限位效果测试完成\n");
    return 0;
}
```

### 6. 多轴限位协调测试

#### 6.1 多轴限位保护协调测试
```cpp
int TestMultiAxisLimitCoordination()
{
    printf("\n=== 多轴限位协调测试 ===\n");

    const int testAxes[] = {1, 2}; // 测试轴1和轴2
    const int axisCount = sizeof(testAxes) / sizeof(testAxes[0]);

    printf("测试轴: ");
    for (int i = 0; i < axisCount; i++)
    {
        printf("%d", testAxes[i]);
        if (i < axisCount - 1) printf(", ");
    }
    printf("\n");

    // 使能所有测试轴
    printf("使能所有测试轴...\n");
    for (int i = 0; i < axisCount; i++)
    {
        g_MultiCard.MC_AxisOn(testAxes[i]);
        Sleep(500);
    }

    // 同时启动多轴运动
    printf("启动多轴同时运动，测试限位协调...\n");

    long targetPositions[] = {80000, 60000}; // 目标位置

    for (int i = 0; i < axisCount; i++)
    {
        printf("  轴%d 目标位置: %ld\n", testAxes[i], targetPositions[i]);
        TestTrapMotion(testAxes[i], targetPositions[i], 15, 0.3, 0.3);
    }

    // 监控多轴运动和限位状态
    DWORD startTime = GetTickCount();
    bool anyLimitTriggered = false;
    bool allAxesStopped = true;
    int stoppedAxes[8] = {0};

    for (int i = 0; i < 2000; i++) // 最多监控20秒
    {
        TAllSysStatusDataSX statusData;
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);

        bool currentAllStopped = true;
        int currentLimitCount = 0;

        // 检查每个轴的状态
        for (int j = 0; j < axisCount; j++)
        {
            int axisIdx = testAxes[j] - 1;
            DWORD axisStatus = statusData.lAxisStatus[axisIdx];

            bool isRunning = (axisStatus & AXIS_STATUS_RUNNING) != 0;
            bool posLimit = (axisStatus & AXIS_STATUS_POS_HARD_LIMIT) != 0;
            bool negLimit = (axisStatus & AXIS_STATUS_NEG_HARD_LIMIT) != 0;

            if (isRunning)
            {
                currentAllStopped = false;
            }
            else if (stoppedAxes[j] == 0)
            {
                stoppedAxes[j] = 1; // 记录轴停止
            }

            if (posLimit || negLimit)
            {
                currentLimitCount++;
                anyLimitTriggered = true;
            }
        }

        // 实时显示状态
        static DWORD lastDisplayTime = 0;
        DWORD currentTime = GetTickCount();
        if (currentTime - lastDisplayTime > 500)
        {
            printf("  多轴状态: 运行中=%s, 限位触发=%d\n",
                   currentAllStopped ? "否" : "是", currentLimitCount);
            lastDisplayTime = currentTime;
        }

        if (currentAllStopped)
        {
            allAxesStopped = true;
            break;
        }

        Sleep(100);
    }

    // 分析多轴协调效果
    DWORD totalTime = GetTickCount() - startTime;
    printf("\n多轴限位协调测试结果:\n");
    printf("  总运行时间: %ld ms\n", totalTime);
    printf("  限位触发: %s\n", anyLimitTriggered ? "是" : "否");
    printf("  所有轴停止: %s\n", allAxesStopped ? "是" : "否");

    if (allAxesStopped)
    {
        printf("✅ 多轴限位协调功能正常\n");
        return 0;
    }
    else
    {
        printf("⚠️ 多轴协调可能存在问题\n");
        return 1;
    }
}
```

### 7. 限位失效安全测试

#### 7.1 限位失效模拟测试
```cpp
int TestLimitFailureSafety()
{
    printf("\n=== 限位失效安全测试 ===\n");
    printf("注意：此测试需要谨慎执行，确保有其他安全保护措施\n");

    // 禁用限位开关（模拟失效）
    printf("模拟限位开关失效...\n");

    const int testAxis = 1;

    // 禁用限位
    g_MultiCard.MC_LmtsOff(testAxis, MC_LIMIT_POSITIVE);
    g_MultiCard.MC_LmtsOff(testAxis, MC_LIMIT_NEGATIVE);

    printf("  正限位已禁用\n");
    printf("  负限位已禁用\n");

    // 确保轴已使能
    g_MultiCard.MC_AxisOn(testAxis);
    Sleep(1000);

    // 测试在限位禁用状态下的运动
    printf("测试限位禁用状态下的运动...\n");
    printf("注意：请密切监控机械位置，准备手动停止\n");

    long extremePosition = 150000; // 极限位置
    printf("尝试移动到极限位置: %ld\n", extremePosition);

    printf("按Enter键开始运动（或输入'skip'跳过此危险测试）...\n");
    char input[20];
    fgets(input, sizeof(input), stdin);
    if (strncmp(input, "skip", 4) == 0)
    {
        printf("跳过限位失效安全测试\n");
        return 1;
    }

    // 启动运动
    TestTrapMotion(testAxis, extremePosition, 10, 0.2, 0.2);

    // 监控运动5秒
    for (int i = 0; i < 50; i++)
    {
        TAllSysStatusDataSX statusData;
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        DWORD axisStatus = statusData.lAxisStatus[testAxis-1];
        long currentPos = statusData.lAxisPrfPos[testAxis-1];

        printf("  位置: %ld, 状态: 运行=%s\n", currentPos,
               (axisStatus & AXIS_STATUS_RUNNING) ? "是" : "否");

        // 检查是否应该手动停止
        if (currentPos > 120000) // 接近机械极限
        {
            printf("⚠️ 接近机械极限，建议手动停止\n");
        }

        Sleep(100);
    }

    // 手动停止
    printf("手动停止运动...\n");
    g_MultiCard.MC_Stop(0xFFFF, 0xFFFF);
    Sleep(1000);

    // 重新启用限位
    printf("重新启用限位开关...\n");
    g_MultiCard.MC_LmtsOn(testAxis, MC_LIMIT_POSITIVE);
    g_MultiCard.MC_LmtsOn(testAxis, MC_LIMIT_NEGATIVE);

    printf("✅ 限位失效安全测试完成\n");
    printf("建议：\n");
    printf("  1. 安装机械挡块作为最后的保护\n");
    printf("  2. 设置合理的软件限位\n");
    printf("  3. 定期检查限位开关功能\n");
    printf("  4. 建立限位状态监控报警\n");

    return 0;
}
```

### 8. 统计分析和报告

#### 8.1 生成限位测试报告
```cpp
int GenerateLimitTestReport()
{
    printf("\n=== 限位控制测试报告 ===\n");

    int totalTestedAxes = 0;
    int workingLimits = 0;
    int enabledLimits = 0;

    printf("各轴限位测试结果:\n");
    for (int i = 0; i < g_limitTestResults.size(); i++)
    {
        const LimitTestResult& result = g_limitTestResults[i];

        if (result.testStatus != "未测试")
        {
            totalTestedAxes++;

            printf("轴%d:\n", result.axisNum);
            printf("  正限位: %s (%s)\n",
                   result.positiveLimitWorking ? "✅ 正常" : "❌ 故障",
                   result.positiveLimitEnabled ? "启用" : "禁用");
            printf("  负限位: %s (%s)\n",
                   result.negativeLimitWorking ? "✅ 正常" : "❌ 故障",
                   result.negativeLimitEnabled ? "启用" : "禁用");

            if (result.positiveLimitWorking) workingLimits++;
            if (result.negativeLimitWorking) workingLimits++;
            if (result.positiveLimitEnabled) enabledLimits++;
            if (result.negativeLimitEnabled) enabledLimits++;

            if (result.positiveLimitPosition != 0)
            {
                printf("  正限位位置: %ld\n", result.positiveLimitPosition);
            }
            if (result.negativeLimitPosition != 0)
            {
                printf("  负限位位置: %ld\n", result.negativeLimitPosition);
            }

            printf("  测试状态: %s\n\n", result.testStatus.c_str());
        }
    }

    printf("总体统计:\n");
    printf("  测试轴数: %d\n", totalTestedAxes);
    printf("  正常限位数: %d\n", workingLimits);
    printf("  启用限位数: %d\n", enabledLimits);

    if (totalTestedAxes > 0)
    {
        double workingRate = (double)workingLimits / (totalTestedAxes * 2) * 100;
        printf("  限位正常率: %.1f%%\n", workingRate);
    }

    // 安全建议
    printf("\n安全建议:\n");
    if (workingLimits < totalTestedAxes * 2)
    {
        printf("  ⚠️ 存在限位故障，建议立即检查和维修\n");
    }
    if (enabledLimits < workingLimits)
    {
        printf("  ⚠️ 部分正常限位被禁用，建议重新启用\n");
    }
    printf("  ✅ 定期检查限位开关功能\n");
    printf("  ✅ 建立限位状态监控报警\n");
    printf("  ✅ 保持机械限位作为后备保护\n");

    return 0;
}
```

### 9. 系统清理和关闭

#### 9.1 安全清理所有轴
```cpp
int SafeShutdownAllAxes()
{
    printf("\n=== 安全关闭所有轴 ===\n");

    // 停止所有运动
    printf("停止所有轴运动...\n");
    g_MultiCard.MC_Stop(0xFFFFFFFF, 0xFFFFFFFF);
    Sleep(1000);

    // 确保所有限位都启用
    printf("启用所有轴的限位保护...\n");
    for (int i = 1; i <= 16; i++)
    {
        g_MultiCard.MC_LmtsOn(i, MC_LIMIT_POSITIVE);
        g_MultiCard.MC_LmtsOn(i, MC_LIMIT_NEGATIVE);
    }

    Sleep(1000);

    // 检查最终状态
    TAllSysStatusDataSX finalStatusData;
    g_MultiCard.MC_GetAllSysStatusSX(&finalStatusData);

    int enabledCount = 0;
    int limitTriggeredCount = 0;

    for (int i = 0; i < 8; i++)
    {
        if (finalStatusData.lAxisStatus[i] & AXIS_STATUS_ENABLE)
        {
            enabledCount++;
        }

        if ((finalStatusData.lAxisStatus[i] & AXIS_STATUS_POS_HARD_LIMIT) ||
            (finalStatusData.lAxisStatus[i] & AXIS_STATUS_NEG_HARD_LIMIT))
        {
            limitTriggeredCount++;
        }
    }

    printf("最终状态:\n");
    printf("  仍使能的轴数: %d\n", enabledCount);
    printf("  触发限位的轴数: %d\n", limitTriggeredCount);

    if (limitTriggeredCount > 0)
    {
        printf("⚠️ 有 %d 个轴仍处于限位状态，请检查机械位置\n", limitTriggeredCount);
    }

    return 0;
}
```

#### 9.2 系统关闭
```cpp
int ShutdownLimitControlSystem()
{
    printf("\n=== 系统关闭 ===\n");

    // 关闭控制卡连接
    g_MultiCard.MC_Close();
    printf("✅ 控制卡连接已关闭\n");

    // 清理测试数据
    g_limitTestResults.clear();

    printf("✅ 限位控制安全功能测试完成\n");
    return 0;
}
```

---

## 🧪 完整测试程序

```cpp
#include <stdio.h>
#include <windows.h>
#include <vector>
#include "MultiCardCPP.h"

#ifdef NDEBUG
#pragma comment(lib,"MultiCard.lib")
#else
#pragma comment(lib,"MultiCard.lib")
#endif

// 全局变量声明（如前文所示）
MultiCard g_MultiCard;
std::vector<LimitTestResult> g_limitTestResults;

// 辅助函数声明
int TestTrapMotion(int axisNum, long targetPos, double vel, double acc, double dec);
int MonitorMotion(int axisNum);

int main()
{
    printf("=== 限位控制安全功能系统完整测试 ===\n\n");

    // 1. 系统初始化
    if (InitializeLimitControlSystem() != 0)
    {
        printf("❌ 系统初始化失败\n");
        return -1;
    }

    // 2. 硬件安全检查
    if (PerformLimitHardwareSafetyCheck() != 0)
    {
        printf("❌ 硬件安全检查失败\n");
        ShutdownLimitControlSystem();
        return -1;
    }

    // 3. 限位开关检测测试
    printf("\n=== 开始限位开关检测测试 ===\n");
    for (int i = 1; i <= 2; i++) // 测试前2轴
    {
        TestLimitSwitchDetection(i);
        Sleep(1000);
    }

    // 4. 限位保护机制测试
    printf("\n=== 开始限位保护机制测试 ===\n");
    for (int i = 1; i <= 2; i++)
    {
        if (g_limitTestResults[i-1].testStatus != "未测试")
        {
            TestLimitAutoStop(i);
            Sleep(1000);
        }
    }

    // 5. 限位恢复测试
    printf("\n=== 开始限位恢复测试 ===\n");
    TestLimitRecovery(1);
    Sleep(1000);

    // 6. 软件限位测试
    printf("\n=== 开始软件限位测试 ===\n");
    TestSoftwareLimitConfiguration(1);
    Sleep(1000);

    // 7. 多轴协调测试
    printf("\n=== 开始多轴协调测试 ===\n");
    TestMultiAxisLimitCoordination();
    Sleep(1000);

    // 8. 安全测试（谨慎执行）
    printf("\n=== 开始安全测试 ===\n");
    printf("警告：以下测试涉及限位失效模拟，请确保有其他安全措施\n");
    printf("是否继续？(y/n): ");
    char choice;
    scanf(" %c", &choice);
    getchar();

    if (choice == 'y' || choice == 'Y')
    {
        TestLimitFailureSafety();
    }
    else
    {
        printf("跳过限位失效安全测试\n");
    }

    // 9. 生成报告
    GenerateLimitTestReport();

    // 10. 安全关闭
    SafeShutdownAllAxes();
    ShutdownLimitControlSystem();

    printf("\n=== 所有限位控制安全功能测试完成 ===\n");
    printf("请根据测试结果检查和维护限位系统\n");
    system("pause");
    return 0;
}
```

---

## 📊 测试结果评估标准

### 功能指标
- **限位检测响应时间**: < 100ms
- **限位触发停止时间**: < 1000ms
- **限位释放检测时间**: < 200ms
- **限位状态准确率**: 100%

### 可靠性指标
- **限位开关寿命**: > 100万次操作
- **限位重复精度**: < ±0.1mm
- **限位故障检测率**: > 99%
- **安全保护成功率**: 100%

### 安全指标
- ✅ 硬限位触发时自动停止
- ✅ 限位失效时的后备保护
- ✅ 多轴限位协调保护
- ✅ 限位状态实时监控

---

## ⚠️ 常见问题排查

### 限位不触发
**可能原因**:
1. 限位开关损坏
2. 接线松动或断路
3. 电源供应异常
4. 信号干扰

**排查步骤**:
```cpp
// 检查原始限位信号
TAllSysStatusDataSX statusData;
g_MultiCard.MC_GetAllSysStatusSX(&statusData);
DWORD rawPosLimit = statusData.lLimitPosRaw;
DWORD rawNegLimit = statusData.lLimitNegRaw;

printf("原始限位信号: 正=%s, 负=%s\n",
       (rawPosLimit & 0x0001) ? "ON" : "OFF",
       (rawNegLimit & 0x0001) ? "ON" : "OFF");

// 使用万用表测量电压
// 检查24V电源和信号线路
```

### 限位触发但不停
**可能原因**:
1. 限位被禁用
2. 轴未使能
3. 参数配置错误
4. 软件bug

**解决方案**:
```cpp
// 检查限位使能状态
short posLimitEnabled, negLimitEnabled;
g_MultiCard.MC_GetLmtsOnOff(axisNum, &posLimitEnabled, &negLimitEnabled);

if (!posLimitEnabled) {
    printf("正限位被禁用，正在启用...\n");
    g_MultiCard.MC_LmtsOn(axisNum, MC_LIMIT_POSITIVE);
}

// 检查轴使能状态
DWORD axisStatus = statusData.lAxisStatus[axisNum-1];
if (!(axisStatus & AXIS_STATUS_ENABLE)) {
    printf("轴未使能，限位保护无效\n");
}
```

### 限位误触发
**可能原因**:
1. 机械振动干扰
2. 电磁干扰
3. 开关触点粘连
4. 安装位置不当

**改善方法**:
- 增加机械阻尼
- 使用屏蔽电缆
- 调整安装位置
- 选择合适的开关类型

---

## 🎯 总结

本测试文档提供了限位控制安全功能的完整验证方案，确保：

### 安全功能完整性
- ✅ 硬件限位检测可靠准确
- ✅ 限位触发自动停止及时
- ✅ 限位恢复功能正常
- ✅ 多轴限位协调保护有效

### 系统可靠性
- ✅ 限位故障检测机制完善
- ✅ 双重限位保护体系健全
- ✅ 安全监控和报警及时
- ✅ 应急处理措施完备

### 实用价值
- 🔧 **安全保障**: 为运动控制系统提供可靠的限位保护
- 📚 **标准参考**: 建立限位系统测试和维护标准
- 🛠️ **故障诊断**: 系统化的限位问题排查方法
- 📈 **预防维护**: 定期检测和预防限位系统故障

通过本测试，可以确保限位控制系统为运动设备提供可靠的安全保护，防止设备损坏和安全事故的发生。

---

**文档版本**: 1.0
**创建日期**: 2025-10-30
**适用硬件**: MultiCard运动控制卡 + 限位开关系统
**测试环境**: Windows + Visual Studio