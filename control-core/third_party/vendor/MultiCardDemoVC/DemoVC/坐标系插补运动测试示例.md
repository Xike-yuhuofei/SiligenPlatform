# 坐标系插补运动测试示例

## 📋 概述

本文档提供运动控制卡坐标系插补运动功能的完整测试示例，包括2D/3D坐标系建立、直线插补、圆弧插补、前瞻控制、缓冲区管理、多轴协调运动、速度覆盖等核心功能的详细验证流程。

## 🎯 测试目标

- ✅ 验证2D/3D坐标系的建立和配置
- ✅ 测试直线插补运动的精度和平滑度
- ✅ 验证圆弧插补运动的圆度和准确性
- ✅ 测试前瞻缓冲区的功能和效果
- ✅ 验证多轴协调运动的同步性
- ✅ 测试速度覆盖实时调整功能
- ✅ 验证缓冲区管理和状态监控
- ✅ 测试复杂轨迹的连续插补能力

## 🔧 Requirements

### 硬件要求
- **运动控制卡**: 已通过基础测试的控制卡
- **伺服系统**: 至少2轴完整的伺服系统（推荐3轴）
- **机械结构**: XY平面或XYZ空间运动机构
- **工作台**: 足够大的运动范围用于测试
- **测量工具**: 千分表、球杆仪或激光干涉仪（可选）

### 机械要求
- **运动范围**: X轴≥200mm, Y轴≥200mm, Z轴≥100mm
- **机械精度**: 定位精度±0.01mm, 重复定位精度±0.005mm
- **刚性要求**: 机械结构刚性良好，无显著变形
- **润滑状态**: 导轨和丝杠润滑良好，运动顺畅

### 软件要求
- **控制卡驱动**: MultiCard.dll V2.1.1.8或更高版本
- **开发环境**: Visual Studio 2010或更高版本
- **依赖库**: MultiCard.lib、MFC运行时库
- **数学库**: 三角函数计算库（用于轨迹验证）

### 安全要求
- **工作空间**: 运动范围内无障碍物和工具
- **限位保护**: 硬限位和软限位配置正确
- **急停功能**: 急停按钮可正常工作
- **负载安全**: 机械负载在伺服额定范围内

### 预备条件
- [ ] 控制卡连接正常且通信稳定
- [ ] 所有测试轴已通过单轴测试
- [ ] 机械结构装配完成，运动顺畅
- [ ] 坐标系方向和原点已定义
- [ ] 安全防护措施到位

---

## 🔄 完整控制流程

### 1. 系统初始化和轴准备

#### 1.1 系统初始化
```cpp
#include "MultiCardCPP.h"
#include <vector>
#include <cmath>
#include <algorithm>

#define PI 3.14159265358979323846
#define FROCAST_LEN 200

MultiCard g_MultiCard;  // 全局控制卡对象
TCrdData LookAheadBuf[FROCAST_LEN]; // 前瞻缓冲区

// 插补测试轨迹结构
struct InterpolationPath {
    std::vector<double> xPoints;
    std::vector<double> yPoints;
    std::vector<double> zPoints;
    std::vector<double> speeds;
    std::vector<double> accelerations;
    std::string pathName;
};

// 测试结果统计
struct TestResults {
    int totalPaths;
    int successfulPaths;
    double totalDistance;
    double totalTime;
    double maxPositionError;
    double maxVelocityError;
    std::vector<std::string> failedPaths;
};

TestResults g_testResults;

int InitializeInterpolationSystem()
{
    printf("=== 坐标系插补运动测试 ===\n\n");

    // 建立控制卡连接
    int result = g_MultiCard.MC_Open(1, "192.168.0.200", 60000, "192.168.0.1", 60000);
    if (result != 0)
    {
        printf("❌ 控制卡连接失败，错误码: %d\n", result);
        return result;
    }

    printf("✅ 控制卡连接成功\n");

    // 复位控制卡
    g_MultiCard.MC_Reset();
    printf("✅ 控制卡复位完成\n");

    // 初始化测试结果
    memset(&g_testResults, 0, sizeof(g_testResults));

    return 0;
}
```

#### 1.2 多轴准备和检查
```cpp
int PrepareAxesForInterpolation()
{
    printf("\n准备插补测试轴...\n");

    // 定义使用的轴
    int axes[] = {1, 2, 3}; // X, Y, Z轴
    const int axisCount = sizeof(axes) / sizeof(axes[0]);

    for (int i = 0; i < axisCount; i++)
    {
        int axisNum = axes[i];
        printf("准备轴%d...\n", axisNum);

        // 检查轴状态
        TAllSysStatusDataSX statusData;
        g_MultiCard.MC_GetAllSysStatusSX(&statusData);
        DWORD axisStatus = statusData.lAxisStatus[axisNum-1];

        if (axisStatus & AXIS_STATUS_SV_ALARM)
        {
            printf("❌ 轴%d存在伺服报警\n", axisNum);
            return -1;
        }

        if (axisStatus & AXIS_STATUS_POS_HARD_LIMIT ||
            axisStatus & AXIS_STATUS_NEG_HARD_LIMIT)
        {
            printf("⚠️ 轴%d触发限位\n", axisNum);
        }

        // 使能轴
        if (!(axisStatus & AXIS_STATUS_ENABLE))
        {
            printf("使能轴%d...\n", axisNum);
            g_MultiCard.MC_AxisOn(axisNum);
            Sleep(1000);

            // 验证使能状态
            g_MultiCard.MC_GetAllSysStatusSX(&statusData);
            if (!(statusData.lAxisStatus[axisNum-1] & AXIS_STATUS_ENABLE))
            {
                printf("❌ 轴%d使能失败\n", axisNum);
                return -1;
            }
        }

        printf("✅ 轴%d准备完成\n", axisNum);
    }

    printf("所有轴准备完成，可以进行插补测试\n");
    return 0;
}
```

### 2. 2D坐标系建立和配置

#### 2.1 基础2D坐标系配置
```cpp
int Setup2DCoordinateSystem()
{
    printf("\n=== 2D坐标系配置 ===\n");

    // 配置坐标系参数
    TCrdPrm crdPrm;
    memset(&crdPrm, 0, sizeof(crdPrm));

    // 基本参数
    crdPrm.dimension = 2;              // 2D坐标系
    crdPrm.evenTime = 0;               // 最小匀速时间
    crdPrm.setOriginFlag = 1;          // 用户指定原点
    crdPrm.originPos[0] = 0;           // X原点
    crdPrm.originPos[1] = 0;           // Y原点
    crdPrm.originPos[2] = 0;           // Z原点

    // 轴映射
    crdPrm.profile[0] = 1;             // X轴映射到轴1
    crdPrm.profile[1] = 2;             // Y轴映射到轴2
    crdPrm.profile[2] = 0;             // Z轴未使用
    crdPrm.profile[3] = 0;
    crdPrm.profile[4] = 0;
    crdPrm.profile[5] = 0;

    // 速度和加速度限制
    crdPrm.synVelMax = 50.0;           // 最大合成速度 50 pulse/ms
    crdPrm.synAccMax = 0.5;            // 最大合成加速度 0.5 pulse/ms²

    printf("坐标系参数:\n");
    printf("  维数: %d\n", crdPrm.dimension);
    printf("  轴映射: X→轴%d, Y→轴%d\n", crdPrm.profile[0], crdPrm.profile[1]);
    printf("  最大速度: %.2f pulse/ms\n", crdPrm.synVelMax);
    printf("  最大加速度: %.2f pulse/ms²\n", crdPrm.synAccMax);

    // 设置坐标系参数
    int result = g_MultiCard.MC_SetCrdPrm(1, &crdPrm);
    if (result != 0)
    {
        printf("❌ 坐标系参数设置失败，错误码: %d\n", result);
        return result;
    }

    printf("✅ 2D坐标系参数设置成功\n");
    return 0;
}
```

#### 2.2 前瞻缓冲区初始化
```cpp
int InitializeLookAheadBuffer()
{
    printf("\n=== 前瞻缓冲区初始化 ===\n");

    // 配置前瞻参数
    TLookAheadPrm lookPrm;
    memset(&lookPrm, 0, sizeof(lookPrm));

    // 缓冲区设置
    lookPrm.lookAheadNum = FROCAST_LEN;  // 前瞻段数
    lookPrm.pLookAheadBuf = LookAheadBuf; // 缓冲区指针

    // 各轴参数设置
    for (int i = 0; i < 8; i++)
    {
        lookPrm.dAccMax[i] = 1.0;       // 最大加速度 1.0 pulse/ms²
        lookPrm.dMaxStepSpeed[i] = 5.0; // 最大速度变化量 5.0 pulse/ms
        lookPrm.dScale[i] = 1.0;        // 脉冲当量 1.0
        lookPrm.dSpeedMax[i] = 60.0;    // 最大速度 60.0 pulse/ms
    }

    printf("前瞻缓冲区参数:\n");
    printf("  缓冲区长度: %d\n", lookPrm.lookAheadNum);
    printf("  最大加速度: %.2f pulse/ms²\n", lookPrm.dAccMax[0]);
    printf("  最大速度: %.2f pulse/ms\n", lookPrm.dSpeedMax[0]);

    // 初始化前瞻缓冲区
    int result = g_MultiCard.MC_InitLookAhead(1, 0, &lookPrm);
    if (result != 0)
    {
        printf("❌ 前瞻缓冲区初始化失败，错误码: %d\n", result);
        return result;
    }

    printf("✅ 前瞻缓冲区初始化成功\n");
    return 0;
}
```

### 3. 直线插补运动测试

#### 3.1 单段直线插补测试
```cpp
int TestSingleLinearInterpolation()
{
    printf("\n=== 单段直线插补测试 ===\n");

    // 测试参数
    long startX = 0, startY = 0;
    long endX = 50000, endY = 30000; // 50mm X 30mm (假设1000 pulse/mm)
    double velocity = 20.0;          // 20 pulse/ms
    double acceleration = 0.3;        // 0.3 pulse/ms²
    double endVelocity = 0.0;         // 结束速度为0

    printf("测试轨迹: (%ld,%ld) → (%ld,%ld)\n", startX, startY, endX, endY);
    printf("运动参数: 速度=%.2f, 加速度=%.2f\n", velocity, acceleration);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 添加直线插补数据
    DWORD startTime = GetTickCount();
    int result = g_MultiCard.MC_LnXY(1, endX, endY, velocity, acceleration,
                                      endVelocity, 0, 1);
    if (result != 0)
    {
        printf("❌ 直线插补数据添加失败，错误码: %d\n", result);
        return result;
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    DWORD dataAddTime = GetTickCount() - startTime;
    printf("数据添加时间: %ld ms\n", dataAddTime);

    // 启动坐标系运动
    printf("启动直线插补运动...\n");
    result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 坐标系运动启动失败，错误码: %d\n", result);
        return result;
    }

    // 监控运动过程
    return MonitorInterpolationMotion("单段直线插补", 30000); // 30秒超时
}
```

#### 3.2 多段连续直线插补测试
```cpp
int TestMultiSegmentLinearInterpolation()
{
    printf("\n=== 多段连续直线插补测试 ===\n");

    // 定义多段路径：方形轨迹
    struct {
        long x, y;
        double vel;
    } pathPoints[] = {
        {0, 0, 20},      // 起点
        {50000, 0, 25},  // 右
        {50000, 30000, 25}, // 上
        {0, 30000, 25},  // 左
        {0, 0, 20}       // 回起点
    };

    const int pointCount = sizeof(pathPoints) / sizeof(pathPoints[0]);
    const double acceleration = 0.3;

    printf("测试轨迹: %d段方形路径\n", pointCount - 1);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 添加每段直线
    DWORD totalDataTime = 0;
    for (int i = 1; i < pointCount; i++)
    {
        printf("添加段%d: (%ld,%ld) → (%ld,%ld), 速度=%.1f\n",
               i, pathPoints[i-1].x, pathPoints[i-1].y,
               pathPoints[i].x, pathPoints[i].y, pathPoints[i].vel);

        DWORD segStartTime = GetTickCount();
        int result = g_MultiCard.MC_LnXY(1, pathPoints[i].x, pathPoints[i].y,
                                          pathPoints[i].vel, acceleration,
                                          0, 0, i);
        totalDataTime += (GetTickCount() - segStartTime);

        if (result != 0)
        {
            printf("❌ 段%d数据添加失败，错误码: %d\n", i, result);
            return result;
        }
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    printf("总数据添加时间: %ld ms\n", totalDataTime);

    // 启动运动
    printf("启动多段直线插补运动...\n");
    int result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 多段运动启动失败，错误码: %d\n", result);
        return result;
    }

    return MonitorInterpolationMotion("多段直线插补", 60000); // 60秒超时
}
```

### 4. 圆弧插补运动测试

#### 4.1 2D圆弧插补基础测试
```cpp
int TestArcInterpolation()
{
    printf("\n=== 2D圆弧插补测试 ===\n");

    // 圆弧参数
    long centerX = 25000, centerY = 15000; // 圆心 (25mm, 15mm)
    long endX = 50000, endY = 15000;       // 终点 (50mm, 15mm)
    long startX = 25000, startY = 0;       // 起点 (25mm, 0mm)
    double radius = 15000.0;               // 半径 15mm
    double velocity = 15.0;                // 速度 15 pulse/ms
    double acceleration = 0.2;              // 加速度
    short direction = 0;                   // 0-逆时针, 1-顺时针

    printf("圆弧参数:\n");
    printf("  圆心: (%ld,%ld)\n", centerX, centerY);
    printf("  起点: (%ld,%ld)\n", startX, startY);
    printf("  终点: (%ld,%ld)\n", endX, endY);
    printf("  半径: %.1f\n", radius);
    printf("  方向: %s\n", direction ? "顺时针" : "逆时针");

    // 计算理论圆弧角度和长度
    double startAngle = atan2(startY - centerY, startX - centerX);
    double endAngle = atan2(endY - centerY, endX - centerX);
    double arcAngle = endAngle - startAngle;
    if (direction == 1) arcAngle = -arcAngle; // 顺时针
    if (arcAngle < 0) arcAngle += 2 * PI;      // 确保正值

    double arcLength = radius * arcAngle;
    printf("  理论圆弧角度: %.2f° (%.3f rad)\n", arcAngle * 180 / PI, arcAngle);
    printf("  理论圆弧长度: %.1f pulse (%.2f mm)\n", arcLength, arcLength / 1000);

    // 移动到起点
    printf("移动到圆弧起点...\n");
    TestTrapMotion(1, startX, 20, 0.5, 0.5);
    TestTrapMotion(2, startY, 20, 0.5, 0.5);
    MonitorMotion(1);
    MonitorMotion(2);
    Sleep(1000);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 添加圆弧插补数据
    printf("添加圆弧插补数据...\n");
    DWORD arcStartTime = GetTickCount();
    int result = g_MultiCard.MC_ArcXYC(1, endX, endY, centerX, centerY,
                                        direction, velocity, acceleration,
                                        0, 0, 1);
    if (result != 0)
    {
        printf("❌ 圆弧插补数据添加失败，错误码: %d\n", result);
        return result;
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    DWORD arcDataTime = GetTickCount() - arcStartTime;
    printf("圆弧数据添加时间: %ld ms\n", arcDataTime);

    // 启动运动
    printf("启动圆弧插补运动...\n");
    result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 圆弧运动启动失败，错误码: %d\n", result);
        return result;
    }

    return MonitorInterpolationMotion("圆弧插补", 45000); // 45秒超时
}
```

#### 4.2 整圆插补测试
```cpp
int TestFullCircleInterpolation()
{
    printf("\n=== 整圆插补测试 ===\n");

    // 整圆参数
    long centerX = 25000, centerY = 15000; // 圆心
    long radius = 20000;                    // 半径 20mm
    double velocity = 12.0;                 // 速度
    double acceleration = 0.15;             // 加速度

    printf("整圆参数:\n");
    printf("  圆心: (%ld,%ld)\n", centerX, centerY);
    printf("  半径: %ld pulse (%.1f mm)\n", radius, radius / 1000.0);

    // 移动到圆周起点
    long startX = centerX + radius;
    long startY = centerY;
    printf("移动到起点 (%ld,%ld)...\n", startX, startY);

    TestTrapMotion(1, startX, 20, 0.5, 0.5);
    TestTrapMotion(2, startY, 20, 0.5, 0.5);
    MonitorMotion(1);
    MonitorMotion(2);
    Sleep(1000);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 添加四个90度圆弧组成整圆
    struct {
        long endX, endY;
        long arcCenterX, arcCenterY;
        short direction;
    } arcs[] = {
        {centerX, centerY + radius, centerX, centerY + radius, 0}, // 上半圆左半
        {centerX - radius, centerY, centerX - radius, centerY, 0},  // 上半圆右半
        {centerX, centerY - radius, centerX, centerY - radius, 0}, // 下半圆左半
        {centerX + radius, centerY, centerX + radius, centerY, 0}  // 下半圆右半
    };

    for (int i = 0; i < 4; i++)
    {
        printf("添加圆弧段%d: 终点(%ld,%ld), 圆心(%ld,%ld)\n",
               i+1, arcs[i].endX, arcs[i].endY,
               arcs[i].arcCenterX, arcs[i].arcCenterY);

        int result = g_MultiCard.MC_ArcXYC(1, arcs[i].endX, arcs[i].endY,
                                            arcs[i].arcCenterX, arcs[i].arcCenterY,
                                            arcs[i].direction, velocity, acceleration,
                                            0, 0, i+1);
        if (result != 0)
        {
            printf("❌ 圆弧段%d添加失败，错误码: %d\n", i+1, result);
            return result;
        }
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    // 启动运动
    printf("启动整圆插补运动...\n");
    int result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 整圆运动启动失败，错误码: %d\n", result);
        return result;
    }

    return MonitorInterpolationMotion("整圆插补", 90000); // 90秒超时
}
```

### 5. 插补运动监控

#### 5.1 运动过程监控
```cpp
int MonitorInterpolationMotion(const char* motionName, int timeoutMs)
{
    printf("监控%s运动过程...\n", motionName);

    DWORD startTime = GetTickCount();
    DWORD lastReportTime = 0;
    double lastPositions[3] = {0, 0, 0};
    double maxVelocity = 0;
    double totalDistance = 0;
    bool motionCompleted = false;

    while ((GetTickCount() - startTime) < timeoutMs)
    {
        // 读取坐标系状态
        short crdStatus;
        double positions[8];
        double currentVelocity;
        long crdSpace, lookaheadSpace;

        int result = g_MultiCard.MC_CrdStatus(1, &crdStatus, NULL);
        result |= g_MultiCard.MC_GetCrdPos(1, positions);
        result |= g_MultiCard.MC_GetCrdVel(1, &currentVelocity);
        result |= g_MultiCard.MC_CrdSpace(1, &crdSpace, 0);
        result |= g_MultiCard.MC_GetLookAheadSpace(1, &lookaheadSpace, 0);

        if (result != 0)
        {
            printf("❌ 读取坐标系状态失败\n");
            return result;
        }

        DWORD currentTime = GetTickCount();

        // 计算瞬时速度
        double instantVelocity = 0;
        for (int i = 0; i < 3; i++)
        {
            double deltaPos = positions[i] - lastPositions[i];
            double deltaTime = (currentTime - lastReportTime) / 1000.0;
            if (deltaTime > 0)
            {
                double axisVelocity = fabs(deltaPos / deltaTime);
                instantVelocity = max(instantVelocity, axisVelocity);
            }
        }

        // 累计移动距离
        double segmentDistance = 0;
        for (int i = 0; i < 3; i++)
        {
            double deltaPos = positions[i] - lastPositions[i];
            segmentDistance += deltaPos * deltaPos;
        }
        totalDistance += sqrt(segmentDistance);

        // 更新最大速度
        maxVelocity = max(maxVelocity, instantVelocity);

        // 定期报告状态
        if (currentTime - lastReportTime > 500) // 每500ms报告一次
        {
            printf("  位置: X=%.1f, Y=%.1f, Z=%.1f\n",
                   positions[0], positions[1], positions[2]);
            printf("  速度: %.2f/%.2f pulse/ms, 缓冲区: %ld/%ld\n",
                   currentVelocity, instantVelocity, crdSpace, lookaheadSpace);

            // 更新上次位置
            for (int i = 0; i < 3; i++)
            {
                lastPositions[i] = positions[i];
            }
            lastReportTime = currentTime;
        }

        // 检查运动完成
        if (!(crdStatus & CRDSYS_STATUS_PROG_RUN) && (crdStatus & CRDSYS_STATUS_FIFO_FINISH_0))
        {
            if (!motionCompleted)
            {
                DWORD totalTime = GetTickCount() - startTime;
                printf("\n✅ %s运动完成\n", motionName);
                printf("  运动时间: %ld ms\n", totalTime);
                printf("  总移动距离: %.1f pulse (%.2f mm)\n",
                       totalDistance, totalDistance / 1000.0);
                printf("  最大速度: %.2f pulse/ms\n", maxVelocity);
                printf("  平均速度: %.2f pulse/ms\n", totalDistance / (totalTime / 1000.0));

                // 验证最终位置
                printf("  最终位置: X=%.1f, Y=%.1f, Z=%.1f\n",
                       positions[0], positions[1], positions[2]);

                motionCompleted = true;

                // 更新测试统计
                g_testResults.totalPaths++;
                g_testResults.successfulPaths++;
                g_testResults.totalDistance += totalDistance;
                g_testResults.totalTime += totalTime;
            }
        }

        // 检查报警状态
        if (crdStatus & CRDSYS_STATUS_ALARM)
        {
            printf("❌ 坐标系报警\n");
            return -1;
        }

        if (motionCompleted)
        {
            // 运动完成后再等待2秒确保稳定
            Sleep(2000);
            break;
        }

        Sleep(50);
    }

    if (!motionCompleted)
    {
        printf("⚠️ %s运动超时\n", motionName);
        g_testResults.totalPaths++;
        g_testResults.failedPaths.push_back(motionName);
        return -2;
    }

    return 0;
}
```

### 6. 速度覆盖功能测试

#### 6.1 实时速度覆盖测试
```cpp
int TestVelocityOverride()
{
    printf("\n=== 速度覆盖功能测试 ===\n");

    // 配置长距离直线运动用于测试
    printf("配置长距离运动用于速度覆盖测试...\n");
    TestTrapMotion(1, 10000, 20, 0.5, 0.5);
    TestTrapMotion(2, 0, 20, 0.5, 0.5);
    MonitorMotion(1);
    MonitorMotion(2);
    Sleep(1000);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 添加长距离直线插补
    long endX = 100000, endY = 80000;
    double normalVelocity = 25.0;
    g_MultiCard.MC_LnXY(1, endX, endY, normalVelocity, 0.3, 0, 0, 1);
    g_MultiCard.MC_CrdData(1, NULL, 0);

    // 启动运动
    printf("启动插补运动...\n");
    g_MultiCard.MC_CrdStart(1, 0);

    // 等待运动开始
    Sleep(2000);

    // 测试不同的速度覆盖比例
    double overrideRatios[] = {0.5, 1.0, 1.5, 2.0, 0.8, 1.0};
    const int ratioCount = sizeof(overrideRatios) / sizeof(overrideRatios[0]);

    for (int i = 0; i < ratioCount; i++)
    {
        printf("设置速度覆盖: %.1f (100%% = 正常速度)\n", overrideRatios[i]);

        double positions[8];
        double beforeVelocity;

        // 记录覆盖前的状态
        g_MultiCard.MC_GetCrdPos(1, positions);
        g_MultiCard.MC_GetCrdVel(1, &beforeVelocity);
        printf("  覆盖前位置: X=%.1f, Y=%.1f, 速度=%.2f\n",
               positions[0], positions[1], beforeVelocity);

        // 设置速度覆盖
        DWORD overrideStartTime = GetTickCount();
        int result = g_MultiCard.MC_SetOverride(1, overrideRatios[i]);
        DWORD overrideEndTime = GetTickCount();

        if (result != 0)
        {
            printf("❌ 速度覆盖设置失败，错误码: %d\n", result);
            continue;
        }

        printf("  覆盖设置时间: %ld ms\n", overrideEndTime - overrideStartTime);

        // 等待速度变化生效
        Sleep(3000);

        // 记录覆盖后的状态
        double afterVelocity;
        g_MultiCard.MC_GetCrdPos(1, positions);
        g_MultiCard.MC_GetCrdVel(1, &afterVelocity);
        printf("  覆盖后位置: X=%.1f, Y=%.1f, 速度=%.2f\n",
               positions[0], positions[1], afterVelocity);

        // 验证覆盖效果
        double expectedVelocity = normalVelocity * overrideRatios[i];
        double velocityError = fabs(afterVelocity - expectedVelocity);
        printf("  期望速度: %.2f, 实际速度: %.2f, 误差: %.2f\n",
               expectedVelocity, afterVelocity, velocityError);

        if (velocityError < expectedVelocity * 0.1) // 10%误差范围内
        {
            printf("  ✅ 速度覆盖效果良好\n");
        }
        else
        {
            printf("  ⚠️ 速度覆盖效果需要改善\n");
        }

        Sleep(2000); // 每次覆盖间隔
    }

    // 恢复正常速度
    printf("恢复正常速度...\n");
    g_MultiCard.MC_SetOverride(1, 1.0);

    // 等待运动完成
    return MonitorInterpolationMotion("速度覆盖测试", 60000);
}
```

### 7. 缓冲区管理测试

#### 7.1 缓冲区容量和性能测试
```cpp
int TestBufferManagement()
{
    printf("\n=== 缓冲区管理测试 ===\n");

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    printf("测试缓冲区数据写入性能...\n");

    const int segmentCount = 100; // 测试100段数据
    double velocities[segmentCount];
    long positions[segmentCount * 2];

    // 生成测试数据：螺旋轨迹
    for (int i = 0; i < segmentCount; i++)
    {
        double angle = 2 * PI * i / segmentCount;
        double radius = 1000 + i * 100; // 螺旋半径递增

        positions[i*2] = (long)(radius * cos(angle));     // X
        positions[i*2+1] = (long)(radius * sin(angle));   // Y
        velocities[i] = 10 + i * 0.1;                     // 速度递增
    }

    // 批量写入测试
    DWORD writeStartTime = GetTickCount();
    int successCount = 0;

    for (int i = 0; i < segmentCount; i++)
    {
        int result = g_MultiCard.MC_LnXY(1, positions[i*2], positions[i*2+1],
                                          velocities[i], 0.2, 0, 0, i+1);
        if (result == 0)
        {
            successCount++;
        }

        // 每10段检查一次缓冲区状态
        if ((i + 1) % 10 == 0)
        {
            long crdSpace, lookaheadSpace;
            g_MultiCard.MC_CrdSpace(1, &crdSpace, 0);
            g_MultiCard.MC_GetLookAheadSpace(1, &lookaheadSpace, 0);

            printf("  段%d: 缓冲区剩余 %ld/%ld\n",
                   i+1, crdSpace, lookaheadSpace);
        }

        // 检查缓冲区是否快满了
        if (i > 0 && i % 20 == 0)
        {
            long crdSpace;
            g_MultiCard.MC_CrdSpace(1, &crdSpace, 0);
            if (crdSpace < 10)
            {
                printf("  缓冲区接近满，暂停写入...\n");
                Sleep(1000);
            }
        }
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    DWORD writeEndTime = GetTickCount();
    DWORD totalWriteTime = writeEndTime - writeStartTime;

    printf("缓冲区写入测试结果:\n");
    printf("  成功写入: %d/%d 段\n", successCount, segmentCount);
    printf("  总写入时间: %ld ms\n", totalWriteTime);
    printf("  平均写入时间: %.2f ms/段\n", (double)totalWriteTime / segmentCount);

    if (successCount == segmentCount)
    {
        printf("✅ 缓冲区管理性能良好\n");

        // 启动运动测试执行效果
        printf("启动螺旋轨迹运动...\n");
        g_MultiCard.MC_CrdStart(1, 0);

        return MonitorInterpolationMotion("缓冲区管理测试", 120000); // 2分钟超时
    }
    else
    {
        printf("⚠️ 缓冲区管理存在问题\n");
        return -1;
    }
}
```

### 8. 3D坐标系插补测试

#### 8.1 3D坐标系配置
```cpp
int Setup3DCoordinateSystem()
{
    printf("\n=== 3D坐标系配置 ===\n");

    // 配置3D坐标系参数
    TCrdPrm crdPrm;
    memset(&crdPrm, 0, sizeof(crdPrm));

    crdPrm.dimension = 3;              // 3D坐标系
    crdPrm.evenTime = 0;
    crdPrm.setOriginFlag = 1;
    crdPrm.originPos[0] = 0;           // X原点
    crdPrm.originPos[1] = 0;           // Y原点
    crdPrm.originPos[2] = 0;           // Z原点

    // 轴映射
    crdPrm.profile[0] = 1;             // X轴映射到轴1
    crdPrm.profile[1] = 2;             // Y轴映射到轴2
    crdPrm.profile[2] = 3;             // Z轴映射到轴3

    // 速度和加速度限制
    crdPrm.synVelMax = 40.0;           // 最大合成速度
    crdPrm.synAccMax = 0.4;            // 最大合成加速度

    printf("3D坐标系参数:\n");
    printf("  维数: %d\n", crdPrm.dimension);
    printf("  轴映射: X→轴%d, Y→轴%d, Z→轴%d\n",
           crdPrm.profile[0], crdPrm.profile[1], crdPrm.profile[2]);
    printf("  最大速度: %.2f pulse/ms\n", crdPrm.synVelMax);
    printf("  最大加速度: %.2f pulse/ms²\n", crdPrm.synAccMax);

    // 设置坐标系参数
    int result = g_MultiCard.MC_SetCrdPrm(1, &crdPrm);
    if (result != 0)
    {
        printf("❌ 3D坐标系参数设置失败，错误码: %d\n", result);
        return result;
    }

    printf("✅ 3D坐标系参数设置成功\n");
    return 0;
}
```

#### 8.2 3D空间螺旋插补测试
```cpp
int Test3DSpiralInterpolation()
{
    printf("\n=== 3D螺旋插补测试 ===\n");

    // 确保Z轴已使能
    TAllSysStatusDataSX statusData;
    g_MultiCard.MC_GetAllSysStatusSX(&statusData);
    if (!(statusData.lAxisStatus[2] & AXIS_STATUS_ENABLE))
    {
        printf("使能Z轴...\n");
        g_MultiCard.MC_AxisOn(3);
        Sleep(2000);
    }

    // 3D螺旋参数
    const int pointCount = 50;
    double baseRadius = 15000.0;      // 基础半径 15mm
    double height = 20000.0;          // 螺旋高度 20mm
    double turns = 3.0;               // 螺旋圈数

    printf("3D螺旋参数:\n");
    printf("  基础半径: %.1f pulse (%.1f mm)\n", baseRadius, baseRadius / 1000);
    printf("  螺旋高度: %.1f pulse (%.1f mm)\n", height, height / 1000);
    printf("  螺旋圈数: %.1f\n", turns);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 生成螺旋轨迹点
    printf("生成3D螺旋轨迹数据...\n");
    for (int i = 0; i < pointCount; i++)
    {
        double t = (double)i / (pointCount - 1);
        double angle = 2 * PI * turns * t;
        double currentRadius = baseRadius * (1 + t * 0.5); // 半径渐增

        long x = (long)(currentRadius * cos(angle));
        long y = (long)(currentRadius * sin(angle));
        long z = (long)(height * t);

        double velocity = 8.0 + t * 4.0; // 速度递增

        int result = g_MultiCard.MC_LnXYZ(1, x, y, z, velocity, 0.2, 0, 0, i+1);
        if (result != 0)
        {
            printf("❌ 3D点%d添加失败，错误码: %d\n", i+1, result);
            return result;
        }

        if ((i + 1) % 10 == 0)
        {
            printf("  已添加点%d: (%ld,%ld,%ld)\n", i+1, x, y, z);
        }
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    printf("3D螺旋数据生成完成\n");

    // 启动运动
    printf("启动3D螺旋插补运动...\n");
    int result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 3D螺旋运动启动失败，错误码: %d\n", result);
        return result;
    }

    return Monitor3DInterpolationMotion("3D螺旋插补", 120000); // 2分钟超时
}
```

#### 8.3 3D运动监控
```cpp
int Monitor3DInterpolationMotion(const char* motionName, int timeoutMs)
{
    printf("监控%s运动过程...\n", motionName);

    DWORD startTime = GetTickCount();
    DWORD lastReportTime = 0;
    double lastPositions[3] = {0, 0, 0};
    double maxVelocity = 0;
    bool motionCompleted = false;

    while ((GetTickCount() - startTime) < timeoutMs)
    {
        // 读取3D坐标系状态
        short crdStatus;
        double positions[8];
        double currentVelocity;

        int result = g_MultiCard.MC_CrdStatus(1, &crdStatus, NULL);
        result |= g_MultiCard.MC_GetCrdPos(1, positions);
        result |= g_MultiCard.MC_GetCrdVel(1, &currentVelocity);

        if (result != 0)
        {
            printf("❌ 读取3D坐标系状态失败\n");
            return result;
        }

        DWORD currentTime = GetTickCount();

        // 计算3D空间速度
        double instantVelocity = 0;
        double deltaTime = (currentTime - lastReportTime) / 1000.0;
        if (deltaTime > 0)
        {
            double dx = positions[0] - lastPositions[0];
            double dy = positions[1] - lastPositions[1];
            double dz = positions[2] - lastPositions[2];
            instantVelocity = sqrt(dx*dx + dy*dy + dz*dz) / deltaTime;
        }

        maxVelocity = max(maxVelocity, instantVelocity);

        // 定期报告3D状态
        if (currentTime - lastReportTime > 500)
        {
            printf("  3D位置: X=%.1f, Y=%.1f, Z=%.1f\n",
                   positions[0], positions[1], positions[2]);
            printf("  3D速度: %.2f/%.2f pulse/ms\n",
                   currentVelocity, instantVelocity);

            // 更新上次位置
            for (int i = 0; i < 3; i++)
            {
                lastPositions[i] = positions[i];
            }
            lastReportTime = currentTime;
        }

        // 检查运动完成
        if (!(crdStatus & CRDSYS_STATUS_PROG_RUN) &&
            (crdStatus & CRDSYS_STATUS_FIFO_FINISH_0))
        {
            if (!motionCompleted)
            {
                DWORD totalTime = GetTickCount() - startTime;
                printf("\n✅ %s运动完成\n", motionName);
                printf("  运动时间: %ld ms\n", totalTime);
                printf("  最大3D速度: %.2f pulse/ms\n", maxVelocity);
                printf("  最终3D位置: X=%.1f, Y=%.1f, Z=%.1f\n",
                       positions[0], positions[1], positions[2]);

                motionCompleted = true;
                g_testResults.totalPaths++;
                g_testResults.successfulPaths++;
            }
        }

        if (motionCompleted)
        {
            Sleep(2000);
            break;
        }

        Sleep(50);
    }

    if (!motionCompleted)
    {
        printf("⚠️ %s运动超时\n", motionName);
        return -1;
    }

    return 0;
}
```

### 9. 复杂轨迹综合测试

#### 9.1 心形轨迹测试
```cpp
int TestHeartShapeInterpolation()
{
    printf("\n=== 心形轨迹插补测试 ===\n");

    const int pointCount = 100;
    double scale = 20000.0; // 缩放因子

    printf("生成心形轨迹数据 (%d个点)...\n", pointCount);

    // 清空缓冲区
    g_MultiCard.MC_CrdClear(1, 0);

    // 生成心形轨迹参数方程
    for (int i = 0; i <= pointCount; i++)
    {
        double t = 2 * PI * i / pointCount;

        // 心形参数方程
        double x = scale * 16 * pow(sin(t), 3);
        double y = scale * (13 * cos(t) - 5 * cos(2*t) - 2 * cos(3*t) - cos(4*t));

        // 归一化到合适范围
        x = x / 16 + 50000; // 中心偏移到(50mm, 30mm)
        y = y / 16 + 30000;

        double velocity = 12.0; // 恒定速度

        int result = g_MultiCard.MC_LnXY(1, (long)x, (long)y,
                                          velocity, 0.15, 0, 0, i+1);
        if (result != 0)
        {
            printf("❌ 心形点%d添加失败，错误码: %d\n", i+1, result);
            return result;
        }

        if ((i + 1) % 20 == 0)
        {
            printf("  已添加点%d: (%.1f, %.1f)\n", i+1, x, y);
        }
    }

    // 结束标识
    g_MultiCard.MC_CrdData(1, NULL, 0);

    printf("心形轨迹数据生成完成\n");

    // 启动运动
    printf("启动心形轨迹插补运动...\n");
    int result = g_MultiCard.MC_CrdStart(1, 0);
    if (result != 0)
    {
        printf("❌ 心形轨迹运动启动失败，错误码: %d\n", result);
        return result;
    }

    return MonitorInterpolationMotion("心形轨迹插补", 180000); // 3分钟超时
}
```

### 10. 系统清理和报告

#### 10.1 测试报告生成
```cpp
int GenerateInterpolationTestReport()
{
    printf("\n=== 插补运动测试报告 ===\n");

    printf("测试统计:\n");
    printf("  总测试路径数: %d\n", g_testResults.totalPaths);
    printf("  成功路径数: %d\n", g_testResults.successfulPaths);
    printf("  失败路径数: %d\n", (int)g_testResults.failedPaths.size());

    if (g_testResults.totalPaths > 0)
    {
        double successRate = (double)g_testResults.successfulPaths / g_testResults.totalPaths * 100;
        printf("  成功率: %.1f%%\n", successRate);
    }

    if (g_testResults.totalDistance > 0)
    {
        printf("性能统计:\n");
        printf("  总移动距离: %.1f pulse (%.2f mm)\n",
               g_testResults.totalDistance, g_testResults.totalDistance / 1000.0);
        printf("  总运动时间: %.1f 秒\n", g_testResults.totalTime / 1000.0);
        printf("  平均速度: %.2f pulse/ms\n",
               g_testResults.totalDistance / (g_testResults.totalTime / 1000.0));
    }

    if (!g_testResults.failedPaths.empty())
    {
        printf("\n失败的测试路径:\n");
        for (const auto& path : g_testResults.failedPaths)
        {
            printf("  - %s\n", path.c_str());
        }
    }

    return 0;
}
```

#### 10.2 系统清理
```cpp
int CleanupInterpolationSystem()
{
    printf("\n=== 插补系统清理 ===\n");

    // 停止所有坐标系运动
    printf("停止所有坐标系运动...\n");
    g_MultiCard.MC_Stop(0XFFFFFF00, 0XFFFFFF00);
    Sleep(1000);

    // 清空所有缓冲区
    printf("清空坐标系缓冲区...\n");
    g_MultiCard.MC_CrdClear(1, 0);

    // 验证停止状态
    short crdStatus;
    g_MultiCard.MC_CrdStatus(1, &crdStatus, NULL);
    if (crdStatus & CRDSYS_STATUS_PROG_RUN)
    {
        printf("⚠️ 坐标系仍在运行状态\n");
    }
    else
    {
        printf("✅ 坐标系已停止\n");
    }

    // 关闭控制卡连接
    g_MultiCard.MC_Close();
    printf("✅ 控制卡连接已关闭\n");

    printf("✅ 坐标系插补运动测试完成\n");
    return 0;
}
```

---

## 🧪 完整测试程序

```cpp
#include <stdio.h>
#include <windows.h>
#include <vector>
#include <cmath>
#include "MultiCardCPP.h"

#ifdef NDEBUG
#pragma comment(lib,"MultiCard.lib")
#else
#pragma comment(lib,"MultiCard.lib")
#endif

// 全局变量声明（如前文所示）
MultiCard g_MultiCard;
TCrdData LookAheadBuf[FROCAST_LEN];
TestResults g_testResults;

// 辅助函数声明
int TestTrapMotion(int axisNum, long targetPos, double vel, double acc, double dec);
int MonitorMotion(int axisNum);

int main()
{
    printf("=== 坐标系插补运动系统完整测试 ===\n\n");

    // 1. 系统初始化
    if (InitializeInterpolationSystem() != 0)
    {
        printf("❌ 系统初始化失败\n");
        return -1;
    }

    // 2. 轴准备
    if (PrepareAxesForInterpolation() != 0)
    {
        printf("❌ 轴准备失败\n");
        CleanupInterpolationSystem();
        return -1;
    }

    // 3. 2D坐标系配置
    if (Setup2DCoordinateSystem() != 0)
    {
        printf("❌ 2D坐标系配置失败\n");
        CleanupInterpolationSystem();
        return -1;
    }

    // 4. 前瞻缓冲区初始化
    if (InitializeLookAheadBuffer() != 0)
    {
        printf("❌ 前瞻缓冲区初始化失败\n");
        CleanupInterpolationSystem();
        return -1;
    }

    // 5. 基础插补测试
    printf("\n=== 开始基础插补测试 ===\n");
    TestSingleLinearInterpolation();
    Sleep(2000);

    TestMultiSegmentLinearInterpolation();
    Sleep(2000);

    TestArcInterpolation();
    Sleep(2000);

    TestFullCircleInterpolation();
    Sleep(2000);

    // 6. 高级功能测试
    printf("\n=== 开始高级功能测试 ===\n");
    TestVelocityOverride();
    Sleep(2000);

    TestBufferManagement();
    Sleep(2000);

    // 7. 3D插补测试
    printf("\n=== 开始3D插补测试 ===\n");
    Setup3DCoordinateSystem();
    Sleep(1000);

    Test3DSpiralInterpolation();
    Sleep(2000);

    // 8. 复杂轨迹测试
    printf("\n=== 开始复杂轨迹测试 ===\n");
    TestHeartShapeInterpolation();
    Sleep(2000);

    // 9. 生成报告
    GenerateInterpolationTestReport();

    // 10. 系统清理
    CleanupInterpolationSystem();

    printf("\n=== 所有坐标系插补运动测试完成 ===\n");
    system("pause");
    return 0;
}
```

---

## 📊 测试结果评估标准

### 精度指标
- **直线插补精度**: 位置误差 < ±10 pulse
- **圆弧插补精度**: 圆度误差 < ±20 pulse
- **3D空间精度**: 综合位置误差 < ±15 pulse
- **速度控制精度**: 速度误差 < ±5%

### 性能指标
- **插补响应时间**: < 50ms
- **数据写入速率**: > 100段/秒
- **前瞻处理延迟**: < 100ms
- **速度覆盖响应**: < 200ms

### 功能指标
- ✅ 支持多段连续插补
- ✅ 前瞻缓冲区平滑过渡
- ✅ 实时速度覆盖调整
- ✅ 复杂轨迹精确跟踪
- ✅ 缓冲区溢出保护

---

## ⚠️ 常见问题排查

### 插补运动不平滑
**可能原因**:
1. 前瞻参数设置不当
2. 加速度过大
3. 缓冲区容量不足
4. 轴参数不匹配

**解决方案**:
```cpp
// 增加前瞻段数
lookPrm.lookAheadNum = 300;

// 调整加速度参数
lookPrm.dAccMax[0] = 0.5; // 降低加速度
```

### 速度覆盖不生效
**可能原因**:
1. 坐标系未运行
2. 覆盖比例超出范围
3. 优先级设置问题

**解决方法**:
```cpp
// 检查坐标系状态
short crdStatus;
g_MultiCard.MC_CrdStatus(1, &crdStatus, NULL);
if (!(crdStatus & CRDSYS_STATUS_PROG_RUN))
{
    printf("坐标系未运行，速度覆盖无效\n");
}

// 设置合理的覆盖比例 (0.1 - 2.0)
double ratio = max(0.1, min(2.0, desiredRatio));
g_MultiCard.MC_SetOverride(1, ratio);
```

### 缓冲区溢出
**可能原因**:
1. 数据写入速度过快
2. 运动速度过低
3. 缓冲区容量不足

**处理方法**:
- 降低数据写入频率
- 增加运动速度
- 扩大缓冲区容量
- 监控缓冲区使用率

---

## 🎯 总结

本测试文档提供了坐标系插补运动功能的完整验证方案，确保：

### 功能完整性
- ✅ 2D/3D坐标系建立正确
- ✅ 直线和圆弧插补精确
- ✅ 前瞻控制平滑有效
- ✅ 多轴协调运动同步

### 技术先进性
- ✅ 实时速度覆盖调整
- ✅ 智能缓冲区管理
- ✅ 复杂轨迹精确跟踪
- ✅ 高性能数据处理

### 实用价值
- 🔧 **精密制造**: 为CNC加工提供插补算法验证
- 📚 **技术学习**: 深入理解插补运动原理和实现
- 🛠️ **系统开发**: 完整的插补控制系统开发指南
- 📈 **性能优化**: 系统化的插补性能调优方法

通过本测试，可以确保坐标系插补运动系统满足精密制造和自动化应用的技术要求，为复杂轨迹控制提供可靠的技术保障。

---

**文档版本**: 1.0
**创建日期**: 2025-10-30
**适用硬件**: MultiCard运动控制卡 + 多轴伺服系统
**测试环境**: Windows + Visual Studio